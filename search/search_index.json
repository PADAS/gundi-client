{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"gundi-client-v2","text":"<p>An async Python client for the Gundi platform.</p> <p>Use this library to authenticate against Gundi, read Integrations and Connections in your account, send Observations and Events, and manage Routes \u2014 from any Python 3.10+ application.</p>"},{"location":"#who-this-is-for","title":"Who this is for","text":"<p>Python developers building third-party apps or utilities that interact with Gundi. You should be comfortable with async Python (<code>asyncio</code>, <code>await</code>) and basic HTTP-API concepts. You do not need prior experience with Gundi, Keycloak, or OAuth2 \u2014 this documentation walks you through it.</p> <p>If you're new to Gundi as a platform, start at gundiservice.org instead.</p>"},{"location":"#whats-inside","title":"What's inside","text":"<ul> <li>Async-first \u2014 all I/O methods are coroutines; use the client as an   async context manager.</li> <li>Three OAuth2 auth paths \u2014 <code>client_credentials</code> (M2M),   <code>password</code> (user-facing), or OIDC discovery against any compliant IdP.</li> <li>Pydantic-modeled responses \u2014 Connections, Integrations, Routes, and   Traces parse into typed objects defined in   <code>gundi-core</code>.</li> <li><code>GundiClient</code> for portal/configuration API calls.</li> <li><code>GundiDataSenderClient</code> for sending observations and events using an   Integration API key.</li> </ul>"},{"location":"#start-here","title":"Start here","text":"<ul> <li>First time? \u2192 Installation \u2192   Authentication setup \u2192   First request</li> <li>Need a refresher on the data model? \u2192   Gundi data model</li> <li>Migrating from 2.x? \u2192 Migration guide</li> <li>Hitting an error? \u2192 Troubleshooting</li> </ul>"},{"location":"#how-this-site-is-organized","title":"How this site is organized","text":"Section What's there Getting started Install, set up credentials, make your first request Concepts Gundi's data model; payload types; how data flows Authentication Detailed coverage of the three OAuth2 flows Reading data Fetching Connections, Integrations, Routes, and Traces Sending data Posting Observations, Events, Messages, and Attachments Recipes Common patterns: pagination, filtering, retries, error handling API reference Auto-generated from in-source docstrings Migration 2.x \u2192 3.0 upgrade guide Troubleshooting Common errors and fixes"},{"location":"changelog/","title":"Changelog","text":"<p>The canonical changelog for <code>gundi-client-v2</code> is the GitHub Releases page.</p> <p>Each release tag (<code>vX.Y.Z</code>) has a description summarizing what changed, linked PRs, and the wheel/sdist artifacts attached.</p> <p>For the latest version notes, follow that link.</p>"},{"location":"changelog/#major-release-migration-guides","title":"Major release migration guides","text":"<ul> <li>2.x \u2192 3.0 \u2014 Migration guide</li> </ul>"},{"location":"migration/","title":"Migration guide","text":""},{"location":"migration/#370-gundi_oauth_-env-var-names","title":"3.7.0 \u2014 <code>GUNDI_OAUTH_*</code> env var names","text":"<p>The preferred env var names for OAuth configuration are now prefixed with <code>GUNDI_</code> to avoid collisions with other tools sharing an environment:</p> Old (still accepted) New (preferred) <code>OAUTH_ISSUER</code> <code>GUNDI_OAUTH_ISSUER</code> <code>OAUTH_TOKEN_URL</code> <code>GUNDI_OAUTH_TOKEN_URL</code> <code>OAUTH_CLIENT_ID</code> <code>GUNDI_OAUTH_CLIENT_ID</code> <code>OAUTH_CLIENT_SECRET</code> <code>GUNDI_OAUTH_CLIENT_SECRET</code> <code>OAUTH_AUDIENCE</code> <code>GUNDI_OAUTH_AUDIENCE</code> <code>OAUTH_SCOPE</code> <code>GUNDI_OAUTH_SCOPE</code> <p>No action is required: the old <code>OAUTH_*</code> names (and the pre-3.0 <code>KEYCLOAK_*</code> names, for the library) keep working as silent fallbacks. When both spellings are set, the <code>GUNDI_</code>-prefixed one wins.</p>"},{"location":"migration/#upgrading-to-30-from-2x","title":"Upgrading to 3.0 from 2.x","text":"<p>This release modernizes the OAuth2 implementation, bumps the <code>httpx</code> runtime dependency, and adds OIDC discovery and routes CRUD. Most consumers will need to update their lockfile and bump a small set of transitive dependencies.</p>"},{"location":"migration/#tldr-what-every-consumer-needs-to-change","title":"TL;DR \u2014 what every consumer needs to change","text":"<ol> <li>Bump the pin in <code>requirements-base.in</code> (or equivalent):    <code>gundi-client-v2~=2.4.0</code> \u2192 <code>gundi-client-v2~=3.0.0</code></li> <li>Bump fastapi and starlette together if you use FastAPI's <code>TestClient</code>    (most integrations do). Minimum compatible quadruple:</li> </ol> Package Minimum Reason <code>gundi-client-v2</code> <code>3.0.0</code> this release <code>httpx</code> <code>0.28</code> required transitively <code>starlette</code> <code>0.37</code> older versions call <code>httpx.Client(app=...)</code>, which <code>httpx&gt;=0.28</code> removed <code>fastapi</code> <code>0.110.3</code> first release that allows <code>starlette&gt;=0.37</code>; still compatible with <code>pydantic~=1.10</code> <ol> <li>Regenerate the lockfile (<code>uv pip compile -o requirements.txt ...</code> or the    equivalent <code>pip-compile</code> invocation).</li> <li>Run your test suite. If a <code>TestClient(app)</code> call raises    <code>TypeError: Client.__init__() got an unexpected keyword argument 'app'</code>,    your starlette is still pinned below 0.37 \u2014 your fastapi bump didn't go    high enough.</li> </ol> <p>A known-good combination tested against <code>gundi-integration-earthranger</code>:</p> <pre><code>gundi-client-v2==3.0.0\nhttpx==0.28.1\nstarlette==0.37.2\nfastapi==0.110.3\npydantic==1.10.26   # still on the v1 line; v2 not required\n</code></pre>"},{"location":"migration/#authentication-uma-ticket-grant-removed","title":"Authentication: uma-ticket grant removed","text":"<p><code>gundi-client-v2</code> no longer uses the Keycloak-specific <code>uma-ticket</code> grant for server-to-server authentication. It now uses the standard OAuth2 <code>client_credentials</code> grant (RFC 6749 \u00a74.4).</p> <p>What this means at the call site: nothing. The client constructor signature is unchanged. <code>GundiClient()</code> reads the same env vars and still authenticates transparently before each request.</p> <p>What this means at the IdP: if your service uses a confidential client configured for the uma-ticket flow specifically, confirm the same client can issue tokens via <code>client_credentials</code>. For Keycloak, this is the default; no change required unless you have explicitly disabled the grant. For Auth0, see the audience note below.</p>"},{"location":"migration/#settings-token-url-derivation-oidc-discovery","title":"Settings: token URL derivation \u2192 OIDC discovery","text":"<p>Old behavior (2.x): the client constructed the token URL at import time:</p> <pre><code># 2.x \u2014 in gundi_client_v2/settings.py\nKEYCLOAK_ISSUER = env.str(\"KEYCLOAK_ISSUER\", None)\nOAUTH_TOKEN_URL = f\"{KEYCLOAK_ISSUER}/protocol/openid-connect/token\"\n</code></pre> <p>This worked only against Keycloak-shaped paths.</p> <p>New behavior (3.0): the client either reads <code>OAUTH_TOKEN_URL</code> directly, or discovers it via OIDC at <code>{OAUTH_ISSUER}/.well-known/openid-configuration</code>. The pre-existing env var names (<code>KEYCLOAK_ISSUER</code>, <code>KEYCLOAK_CLIENT_ID</code>, <code>KEYCLOAK_CLIENT_SECRET</code>, <code>KEYCLOAK_AUDIENCE</code>) still work \u2014 they're aliased to the corresponding <code>OAUTH_*</code> names.</p> <p>Migration paths, in order of preference:</p> <ol> <li>No changes needed (recommended) if your deployment already exports    <code>KEYCLOAK_ISSUER</code> and your IdP serves a well-formed    <code>/.well-known/openid-configuration</code> document (Keycloak, Auth0, Okta, Azure    AD, etc.). The client will discover the token endpoint at runtime, validate    the <code>issuer</code> claim per OIDC Discovery 1.0 \u00a74.3, and cache the result for    the process lifetime.</li> <li>Set <code>OAUTH_TOKEN_URL</code> directly if your IdP doesn't expose a discovery    document or you want to skip the one-time discovery request. This overrides    discovery when set.</li> </ol> <p>What does NOT work anymore: setting only <code>KEYCLOAK_AUTH_SERVICE</code> and <code>KEYCLOAK_REALM</code> in env (without <code>KEYCLOAK_ISSUER</code> or <code>OAUTH_ISSUER</code> or <code>OAUTH_TOKEN_URL</code>). The 2.x library did not read those two env vars either \u2014 your existing deployment must be exporting <code>KEYCLOAK_ISSUER</code> already, or 2.x authentication wouldn't have worked.</p>"},{"location":"migration/#oauth-audience-oauth_audience-keycloak_audience","title":"OAuth audience (<code>OAUTH_AUDIENCE</code> / <code>KEYCLOAK_AUDIENCE</code>)","text":"<p>This parameter is IdP-dependent:</p> <ul> <li>Auth0 password grant: required. Without it, Auth0 issues an opaque token   that cannot be used as an API access token.</li> <li>Keycloak password grant: ignored. The <code>aud</code> claim is determined by the   client configuration in the realm.</li> <li>Other IdPs: consult their documentation. RFC 8707 specifies the   <code>resource</code> parameter as the standard alternative for resource indicators;   some IdPs accept either.</li> </ul> <p>The library passes whatever you set; it does not interpret the value.</p>"},{"location":"migration/#new-capabilities-opt-in","title":"New capabilities (opt-in)","text":"<p>These are available after upgrade but don't require changes to existing code:</p> <ul> <li>OIDC discovery via <code>OAUTH_ISSUER</code> env var or the <code>oauth_issuer</code> kwarg   to <code>GundiClient(...)</code>. See <code>gundi_client_v2/auth.py::discover_token_endpoint</code>.</li> <li>Routes CRUD: <code>get_routes()</code>, <code>get_route_details(route_id)</code>,   <code>get_routes_for_connection(connection_id)</code>, <code>create_route(data)</code>,   <code>update_route(route_id, data)</code>, <code>delete_route(route_id)</code>.</li> <li>Password grant for user-facing flows (existing in 2.x, refined in 3.0).   See <code>examples/list_connections_password_grant.py</code>.</li> <li>Refresh token rotation handled per RFC 6749 \u00a76 (server may omit the   rotated refresh token; the client reuses the cached one).</li> </ul>"},{"location":"migration/#troubleshooting","title":"Troubleshooting","text":"<p><code>TypeError: Client.__init__() got an unexpected keyword argument 'app'</code> Your starlette is below 0.37 but httpx is 0.28+. Bump fastapi to <code>&gt;=0.110.3</code> and re-resolve.</p> <p><code>pip</code> resolver fails to find a satisfying set of versions Most often: your lockfile pins <code>httpcore==0.17.x</code> (paired with httpx 0.24). Run a fresh resolve, don't try to install on top of the old lockfile.</p> <p><code>AuthenticationError: OIDC discovery failed for ...</code> The client tried to discover the token endpoint and got a network error. Check network reachability to your IdP and that <code>{OAUTH_ISSUER}/.well-known/openid-configuration</code> returns a 200. As a fallback, set <code>OAUTH_TOKEN_URL</code> directly.</p> <p><code>AuthenticationError: ...returned issuer ...does not match the expected issuer</code> The discovery document's <code>issuer</code> claim doesn't match the URL the client used to fetch it. This is the OIDC Discovery 1.0 \u00a74.3 validation. Common causes: trailing slash differences (the client normalizes via <code>rstrip('/')</code> on both sides, so this shouldn't trip you up), or an IdP misconfiguration.</p> <p>Authentication worked in 2.x but fails in 3.0 with no obvious error Check that the deployment env actually exports <code>KEYCLOAK_ISSUER</code> (or <code>OAUTH_ISSUER</code> or <code>OAUTH_TOKEN_URL</code>). Some consumer settings modules compose <code>KEYCLOAK_ISSUER</code> from <code>KEYCLOAK_AUTH_SERVICE + KEYCLOAK_REALM</code> as a Python local variable, which the client cannot see.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Common errors when using <code>gundi-client-v2</code>, with their causes and fixes.</p>"},{"location":"troubleshooting/#authentication-errors","title":"Authentication errors","text":"<p>See Authentication \u2192 Refresh and errors for the full catalog with explanations.</p>"},{"location":"troubleshooting/#httpxconnecterror-errno-2-name-or-service-not-known","title":"<code>httpx.ConnectError: [Errno -2] Name or service not known</code>","text":"<p>The hostname in <code>GUNDI_API_BASE_URL</code> or <code>GUNDI_OAUTH_ISSUER</code> doesn't resolve.</p> <p>Fix: Verify the URLs are correct and reachable from the host running your code. Common gotchas:</p> <ul> <li>Typos in the host (<code>gundiservice.org</code> vs <code>gundiserice.org</code>)</li> <li>Missing <code>https://</code> scheme</li> <li>Internal hostnames that don't resolve outside a VPN</li> </ul>"},{"location":"troubleshooting/#httpxconnecttimeout-or-httpxreadtimeout","title":"<code>httpx.ConnectTimeout</code> or <code>httpx.ReadTimeout</code>","text":"<p>The server isn't responding within the configured timeout. The defaults are 3.1 seconds for <code>connect</code> and 20 seconds for <code>data</code> (read).</p> <p>Fix: Increase timeouts at construction time using the <code>connect_timeout</code> and <code>data_timeout</code> kwargs:</p> <pre><code>from gundi_client_v2 import GundiClient\n\nclient = GundiClient(\n    connect_timeout=60.0,  # seconds to establish the connection\n    data_timeout=60.0,     # seconds to read the response body\n)\n</code></pre>"},{"location":"troubleshooting/#typeerror-client__init__-got-an-unexpected-keyword-argument-app","title":"<code>TypeError: Client.__init__() got an unexpected keyword argument 'app'</code>","text":"<p>You're on <code>httpx&gt;=0.28</code> but <code>starlette&lt;0.37</code>. This usually surfaces in FastAPI test code (<code>TestClient(app)</code>).</p> <p>Fix: See Migration \u2192 Troubleshooting. Short version: bump <code>fastapi</code> to <code>&gt;=0.110.3</code> (which allows <code>starlette&gt;=0.37</code>).</p>"},{"location":"troubleshooting/#validationerror-from-pydantic-when-parsing-a-response","title":"<code>ValidationError</code> from Pydantic when parsing a response","text":"<p>The API returned a payload that doesn't match the expected <code>gundi-core</code> schema. This typically happens during version skew (your client and the server are out of sync) or for newly-introduced fields.</p> <p>Fix:</p> <ul> <li>Confirm the deployed Gundi version matches your client major version.</li> <li>If the field is benign, you may need to upgrade <code>gundi-core</code> to a release   that knows about it.</li> <li>If you suspect a server bug, capture the raw response by intercepting   the HTTPX transport and report it to your Gundi administrator.</li> </ul>"},{"location":"troubleshooting/#gundiapierror-http-403","title":"<code>GundiAPIError: HTTP 403</code>","text":"<p>The authenticated principal doesn't have permission to access the requested resource.</p> <p>Fix: Check that your client/user has the necessary roles in the IdP realm. For per-Integration resources, your principal must own or be granted access to that Integration's Organization.</p>"},{"location":"troubleshooting/#gundiapierror-http-404","title":"<code>GundiAPIError: HTTP 404</code>","text":"<p>The resource doesn't exist or you don't have permission to see it (some APIs return 404 instead of 403 to avoid leaking resource existence).</p> <p>Fix: Verify the ID is correct. If you have an <code>integration_id</code> from elsewhere, confirm it actually exists with <code>client.get_integration_details(integration_id)</code>.</p>"},{"location":"troubleshooting/#stuck","title":"Stuck?","text":"<ul> <li>Need patterns for catching exceptions and logging? See   Recipes \u2192 Error handling.</li> <li>Check the Authentication overview for   grant-selection issues.</li> <li>Check the Migration guide if you recently upgraded.</li> <li>Open an issue at   github.com/PADAS/gundi-client/issues.</li> </ul>"},{"location":"api-reference/","title":"API reference","text":"<p>Auto-generated from the in-source docstrings of <code>gundi_client_v2</code>. For conceptual and tutorial content see the rest of the site; this section is a method-by-method reference.</p> Module Contents GundiClient Portal/configuration API client (auth, Connections, Integrations, Routes, Traces). GundiDataSenderClient Sender for Observations, Events, Messages, and Attachments using an Integration API key. auth Standalone OAuth2 grant and OIDC discovery functions. errors Exception hierarchy: <code>GundiClientError</code>, <code>AuthenticationError</code>, <code>GundiAPIError</code>."},{"location":"api-reference/auth/","title":"auth","text":"<p>Standalone OAuth2 helpers. You normally don't call these directly \u2014 <code>GundiClient</code> orchestrates them \u2014 but they're available if you need fine-grained control.</p>"},{"location":"api-reference/auth/#gundi_client_v2.auth","title":"gundi_client_v2.auth","text":""},{"location":"api-reference/auth/#gundi_client_v2.auth.get_access_token_client_credentials","title":"get_access_token_client_credentials  <code>async</code>","text":"<pre><code>get_access_token_client_credentials(session: AsyncClient, oauth_token_url: str, client_id: str, client_secret: str, audience: str | None = None, scope: str = 'openid') -&gt; OAuthToken\n</code></pre> <p>Obtain an access token via the OAuth2 client_credentials grant (RFC 6749 \u00a74.4).</p> <p>Intended for confidential clients (server-to-server). Responses for this grant type typically do NOT include a refresh token (RFC 6749 \u00a74.4.3 says SHOULD NOT). Empty values are backfilled so the OAuthToken schema (which requires both fields) still parses; the caller treats <code>refresh_token=\"\"</code> and <code>refresh_expires_in=0</code> as \"no refresh available\" and re-authenticates on each access-token expiry.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncClient</code> <p>An <code>httpx.AsyncClient</code> (or compatible) used for the HTTP request.</p> required <code>oauth_token_url</code> <code>str</code> <p>The token endpoint URL of the authorization server.</p> required <code>client_id</code> <code>str</code> <p>The OAuth2 client identifier registered with the IdP.</p> required <code>client_secret</code> <code>str</code> <p>The client secret for authenticating the request.</p> required <code>audience</code> <code>str | None</code> <p>Optional audience string required by some IdPs (e.g. Auth0). Omit for IdPs that do not accept this parameter (e.g. Keycloak).</p> <code>None</code> <code>scope</code> <code>str</code> <p>Space-separated OAuth2 scopes to request. Defaults to <code>\"openid\"</code>.</p> <code>'openid'</code> <p>Returns:</p> Type Description <code>OAuthToken</code> <p>An <code>OAuthToken</code> containing the access token</p> <code>OAuthToken</code> <p>and related metadata. <code>refresh_token</code> is <code>\"\"</code> and</p> <code>OAuthToken</code> <p><code>refresh_expires_in</code> is <code>0</code> when the IdP did not issue a refresh token.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the token endpoint returns a non-2xx response.</p>"},{"location":"api-reference/auth/#gundi_client_v2.auth.get_access_token_password_grant","title":"get_access_token_password_grant  <code>async</code>","text":"<pre><code>get_access_token_password_grant(session: AsyncClient, oauth_token_url: str, client_id: str, username: str, password: str, audience: str | None = None, scope: str = 'openid') -&gt; OAuthToken\n</code></pre> <p>Obtain an access token via the OAuth2 Resource Owner Password Credentials grant.</p> <p>This grant is deprecated in OAuth 2.1 (RFC 9700) and should be avoided for new integrations. It is retained for backward compatibility with IdPs that do not support client_credentials for the required scopes.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncClient</code> <p>An <code>httpx.AsyncClient</code> (or compatible) used for the HTTP request.</p> required <code>oauth_token_url</code> <code>str</code> <p>The token endpoint URL of the authorization server.</p> required <code>client_id</code> <code>str</code> <p>The OAuth2 client identifier registered with the IdP.</p> required <code>username</code> <code>str</code> <p>The resource owner's username.</p> required <code>password</code> <code>str</code> <p>The resource owner's password.</p> required <code>audience</code> <code>str | None</code> <p>Optional audience string required by some IdPs (e.g. Auth0). Omit for IdPs that do not accept this parameter (e.g. Keycloak).</p> <code>None</code> <code>scope</code> <code>str</code> <p>Space-separated OAuth2 scopes to request. Defaults to <code>\"openid\"</code>.</p> <code>'openid'</code> <p>Returns:</p> Type Description <code>OAuthToken</code> <p>An <code>OAuthToken</code> containing the access token</p> <code>OAuthToken</code> <p>and related metadata.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the token endpoint returns a non-2xx response.</p>"},{"location":"api-reference/auth/#gundi_client_v2.auth.refresh_access_token","title":"refresh_access_token  <code>async</code>","text":"<pre><code>refresh_access_token(session: AsyncClient, oauth_token_url: str, client_id: str, refresh_token: str, fallback: OAuthToken, client_secret: str | None = None, scope: str = 'openid') -&gt; tuple[OAuthToken, bool]\n</code></pre> <p>Exchange a refresh token for a new access token (RFC 6749 \u00a76).</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncClient</code> <p>An <code>httpx.AsyncClient</code> (or compatible) used for the HTTP request.</p> required <code>oauth_token_url</code> <code>str</code> <p>The token endpoint URL of the authorization server.</p> required <code>client_id</code> <code>str</code> <p>The OAuth2 client identifier registered with the IdP.</p> required <code>refresh_token</code> <code>str</code> <p>The refresh token obtained from a previous token response.</p> required <code>fallback</code> <code>OAuthToken</code> <p>The previous <code>OAuthToken</code> used to backfill <code>refresh_token</code> and <code>refresh_expires_in</code> if the IdP response omits them (RFC 6749 \u00a76 makes the new refresh token OPTIONAL).</p> required <code>client_secret</code> <code>str | None</code> <p>Optional client secret for confidential clients.</p> <code>None</code> <code>scope</code> <code>str</code> <p>Space-separated OAuth2 scopes to request. Defaults to <code>\"openid\"</code>.</p> <code>'openid'</code> <p>Returns:</p> Type Description <code>OAuthToken</code> <p>A tuple <code>(token, refresh_rotated)</code> where <code>token</code> is a new</p> <code>bool</code> <p><code>OAuthToken</code> and <code>refresh_rotated</code> is</p> <code>tuple[OAuthToken, bool]</code> <p><code>True</code> when the IdP issued a new refresh token in the response,</p> <code>tuple[OAuthToken, bool]</code> <p><code>False</code> when it omitted one (the fallback refresh metadata is reused).</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the token endpoint returns a non-2xx response.</p>"},{"location":"api-reference/auth/#gundi_client_v2.auth.discover_token_endpoint","title":"discover_token_endpoint  <code>async</code>","text":"<pre><code>discover_token_endpoint(session: AsyncClient, issuer: str) -&gt; str\n</code></pre> <p>Fetch the OIDC discovery document and return the token endpoint URL.</p> <p>Fetches <code>{issuer}/.well-known/openid-configuration</code> and extracts <code>token_endpoint</code>. Results are cached per-issuer for the process lifetime; call <code>clear_discovery_cache()</code> to invalidate.</p> <p>The cache key is <code>issuer.rstrip('/')</code> so values differing only by a trailing slash share one cache entry. The same normalization is applied when comparing the returned <code>issuer</code> claim to the expected value.</p> <p>Per OIDC Discovery 1.0 \u00a74.3, the <code>issuer</code> field in the discovery document MUST match the URL used to fetch it; a mismatch raises <code>AuthenticationError</code> to prevent credential redirection to a foreign token endpoint.</p> <p>Parameters:</p> Name Type Description Default <code>session</code> <code>AsyncClient</code> <p>An <code>httpx.AsyncClient</code> (or compatible) used for the HTTP request.</p> required <code>issuer</code> <code>str</code> <p>The base URL of the OpenID Provider (e.g. <code>https://auth.example.com/realms/my-realm</code>). A trailing slash is accepted and normalized away.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The <code>token_endpoint</code> URL string from the discovery document.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the discovery request fails, the response is not valid JSON, the JSON is not an object, the <code>issuer</code> claim does not match the expected value, or <code>token_endpoint</code> is absent or not a string.</p>"},{"location":"api-reference/auth/#gundi_client_v2.auth.clear_discovery_cache","title":"clear_discovery_cache","text":"<pre><code>clear_discovery_cache() -&gt; None\n</code></pre> <p>Clear the OIDC discovery cache. Useful for tests and for long-running processes that need to pick up an IdP configuration change without a restart.</p>"},{"location":"api-reference/data-sender-client/","title":"GundiDataSenderClient","text":""},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient","title":"gundi_client_v2.client.GundiDataSenderClient","text":""},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient.__init__","title":"__init__","text":"<pre><code>__init__(integration_api_key: Optional[str] = None, **kwargs: Any)\n</code></pre> <p>Initialize the data-sender client for posting payloads to Gundi.</p> <p>Handles observations, events, messages, and event attachments. This client authenticates using an integration API key rather than OAuth and is intended for integrations that push data into Gundi via the sensors API. Obtain the key from <code>GundiClient.get_integration_api_key(integration_id)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>integration_api_key</code> <code>Optional[str]</code> <p>The per-integration API key used as the <code>apikey</code> HTTP header on every request. May be left <code>None</code> at construction time (e.g. to be set later), but every method that issues a request will raise <code>ValueError</code> if the key is still missing when called.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Optional keyword overrides.</p> <ul> <li><code>sensors_api_base_url</code> (str): Override the sensors API   base URL. Falls back to the <code>SENSORS_API_BASE_URL</code>   environment variable.</li> </ul> <code>{}</code>"},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient.post_observations","title":"post_observations  <code>async</code>","text":"<pre><code>post_observations(data: List[dict]) -&gt; Any\n</code></pre> <p>Post a batch of observation records to Gundi.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>List[dict]</code> <p>List of observation dicts. Each dict should conform to the Gundi observation schema. Non-serialisable values (e.g. <code>datetime</code> objects) are automatically coerced to strings.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The raw JSON response from the sensors API. Typically a list of</p> <code>Any</code> <p>envelopes, one per posted record.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no <code>integration_api_key</code> was provided.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient.post_events","title":"post_events  <code>async</code>","text":"<pre><code>post_events(data: List[dict]) -&gt; Any\n</code></pre> <p>Post a batch of event records to Gundi.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>List[dict]</code> <p>List of event dicts. Each dict should conform to the Gundi event schema. Non-serialisable values are automatically coerced to strings.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The raw JSON response from the sensors API. Typically a list of</p> <code>Any</code> <p>envelopes, one per posted record.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no <code>integration_api_key</code> was provided.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient.post_messages","title":"post_messages  <code>async</code>","text":"<pre><code>post_messages(data: List[dict]) -&gt; Any\n</code></pre> <p>Post a batch of message records to Gundi.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>List[dict]</code> <p>List of message dicts. Each dict should conform to the Gundi message schema. Non-serialisable values are automatically coerced to strings.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The raw JSON response from the sensors API. Typically a list of</p> <code>Any</code> <p>envelopes, one per posted record.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no <code>integration_api_key</code> was provided.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient.update_event","title":"update_event  <code>async</code>","text":"<pre><code>update_event(event_id: str, data: dict) -&gt; dict\n</code></pre> <p>Partially update an existing event via HTTP PATCH.</p> <p>Only the fields present in <code>data</code> are modified; omitted fields retain their current values on the server (standard PATCH semantics).</p> <p>Parameters:</p> Name Type Description Default <code>event_id</code> <code>str</code> <p>The UUID (or string ID) of the event to update.</p> required <code>data</code> <code>dict</code> <p>Dict of fields to update. Non-serialisable values are automatically coerced to strings.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>The raw JSON response body returned by the sensors API,</p> <code>dict</code> <p>representing the updated event.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no <code>integration_api_key</code> was provided.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/data-sender-client/#gundi_client_v2.client.GundiDataSenderClient.post_event_attachments","title":"post_event_attachments  <code>async</code>","text":"<pre><code>post_event_attachments(event_id: str, attachments: List[tuple]) -&gt; Any\n</code></pre> <p>Upload file attachments for an existing event via multipart POST.</p> <p>Parameters:</p> Name Type Description Default <code>event_id</code> <code>str</code> <p>The UUID (or string ID) of the event to attach files to.</p> required <code>attachments</code> <code>List[tuple]</code> <p>List of <code>(filename, file_binary)</code> tuples where <code>filename</code> is a string (e.g. <code>\"photo.jpg\"</code>) and <code>file_binary</code> is the raw bytes of the file.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>The raw JSON response from the sensors API. Typically a list of</p> <code>Any</code> <p>envelopes, one per posted record.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no <code>integration_api_key</code> was provided.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/errors/","title":"errors","text":"<p>Exception classes raised by the library, plus the <code>raise_for_status</code> helper.</p>"},{"location":"api-reference/errors/#gundi_client_v2.errors","title":"gundi_client_v2.errors","text":""},{"location":"api-reference/errors/#gundi_client_v2.errors.GundiClientError","title":"GundiClientError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for the Gundi client.</p>"},{"location":"api-reference/errors/#gundi_client_v2.errors.AuthenticationError","title":"AuthenticationError","text":"<p>               Bases: <code>GundiClientError</code></p> <p>Raised when OAuth token retrieval or authentication fails.</p>"},{"location":"api-reference/errors/#gundi_client_v2.errors.GundiAPIError","title":"GundiAPIError","text":"<p>               Bases: <code>GundiClientError</code></p> <p>Raised when the Gundi API returns a 4xx or 5xx HTTP response.</p> <p>Attributes:</p> Name Type Description <code>status_code</code> <p>The HTTP status code from the response.</p> <code>detail</code> <p>The response body text (often the API's error description), or an empty string when the response carried no body.</p>"},{"location":"api-reference/errors/#gundi_client_v2.errors.raise_for_status","title":"raise_for_status","text":"<pre><code>raise_for_status(response)\n</code></pre> <p>Raise GundiAPIError for a 4xx/5xx httpx response, preserving the original error.</p> <p>Mirrors httpx.Response.raise_for_status(), which raises only for client/server error statuses (3xx redirects do not raise).</p>"},{"location":"api-reference/gundi-client/","title":"GundiClient","text":""},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient","title":"gundi_client_v2.client.GundiClient","text":""},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.__init__","title":"__init__","text":"<pre><code>__init__(**kwargs: Any)\n</code></pre> <p>Initialize the Gundi API client.</p> <p>All parameters are optional and fall back to the corresponding environment variable when not supplied.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <code>Any</code> <p>Keyword overrides for client behaviour.</p> <p>API settings</p> <ul> <li><code>base_url</code> (str): Gundi API base URL. Env:   <code>GUNDI_API_BASE_URL</code>.</li> <li><code>use_ssl</code> (bool): Whether to verify TLS certificates.   Env: <code>GUNDI_API_SSL_VERIFY</code> (default <code>True</code>).</li> </ul> <p>Authentication settings</p> <p>The preferred kwarg names use the <code>oauth_</code> prefix. The legacy <code>keycloak_</code> prefix is still accepted for backward compatibility and is silently mapped to the same setting.</p> <ul> <li><code>oauth_client_id</code> / <code>keycloak_client_id</code> (str):   OAuth client ID. Env: <code>GUNDI_OAUTH_CLIENT_ID</code> /   <code>OAUTH_CLIENT_ID</code> / <code>KEYCLOAK_CLIENT_ID</code>.</li> <li><code>oauth_client_secret</code> / <code>keycloak_client_secret</code>   (str): OAuth client secret (confidential clients). Env:   <code>GUNDI_OAUTH_CLIENT_SECRET</code> / <code>OAUTH_CLIENT_SECRET</code> /   <code>KEYCLOAK_CLIENT_SECRET</code>.</li> <li><code>username</code> (str): Resource-owner username (password   grant). Env: <code>GUNDI_USERNAME</code>.</li> <li><code>password</code> (str): Resource-owner password (password   grant). Env: <code>GUNDI_PASSWORD</code>.</li> <li><code>oauth_token_url</code> (str): Direct token endpoint URL.   Takes precedence over <code>oauth_issuer</code>. Env:   <code>GUNDI_OAUTH_TOKEN_URL</code> / <code>OAUTH_TOKEN_URL</code>.</li> <li><code>oauth_issuer</code> (str): OIDC issuer URL. Used for   automatic token-endpoint discovery when   <code>oauth_token_url</code> is not set. Env:   <code>GUNDI_OAUTH_ISSUER</code> / <code>OAUTH_ISSUER</code> /   <code>KEYCLOAK_ISSUER</code>.</li> <li><code>oauth_audience</code> / <code>keycloak_audience</code> (str): OAuth   audience claim. Required by Auth0; optional for   Keycloak. Env: <code>GUNDI_OAUTH_AUDIENCE</code> /   <code>OAUTH_AUDIENCE</code> / <code>KEYCLOAK_AUDIENCE</code>.</li> <li><code>oauth_scope</code> (str): Space-separated OAuth scopes.   Env: <code>GUNDI_OAUTH_SCOPE</code> / <code>OAUTH_SCOPE</code> (default   <code>\"openid\"</code>).</li> </ul> <p>Retry / timeout settings</p> <ul> <li><code>max_http_retries</code> (int): Number of automatic HTTP   retries on transport errors (default <code>5</code>).</li> <li><code>connect_timeout</code> (float): TCP connect timeout in   seconds (default <code>3.1</code>).</li> <li><code>data_timeout</code> (float): Read/write timeout in seconds   (default <code>20</code>).</li> </ul> <code>{}</code>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.close","title":"close  <code>async</code>","text":"<pre><code>close()\n</code></pre> <p>Close the underlying HTTPX async session.</p> <p>Call this when you are finished with the client and are not using it as an async context manager. After calling <code>close()</code>, the client should not be used again.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.__aenter__","title":"__aenter__  <code>async</code>","text":"<pre><code>__aenter__()\n</code></pre> <p>Enter the async context manager, returning this client instance.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.__aexit__","title":"__aexit__  <code>async</code>","text":"<pre><code>__aexit__(exc_type, exc_value, traceback)\n</code></pre> <p>Exit the async context manager, closing the underlying session.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_access_token","title":"get_access_token  <code>async</code>","text":"<pre><code>get_access_token(force_refresh_token: bool = False) -&gt; OAuthToken\n</code></pre> <p>Return a valid OAuth access token, refreshing it when necessary.</p> <p>The token is cached in memory. On each call the expiry time is checked (with a 15-second clock-skew buffer). If the token has expired \u2014 or <code>force_refresh_token</code> is <code>True</code> \u2014 a new token is fetched from the IdP using the refresh-token grant (when a live refresh token is available) or a fresh full authentication.</p> <p>Parameters:</p> Name Type Description Default <code>force_refresh_token</code> <code>bool</code> <p>When <code>True</code>, bypass the cache and always fetch a fresh token from the IdP, even if the cached token has not yet expired.</p> <code>False</code> <p>Returns:</p> Type Description <code>OAuthToken</code> <p>A valid <code>OAuthToken</code> object containing <code>access_token</code>,</p> <code>OAuthToken</code> <p><code>token_type</code>, <code>expires_in</code>, and related fields.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If token retrieval fails (bad credentials, unreachable IdP, or missing configuration).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_auth_header","title":"get_auth_header  <code>async</code>","text":"<pre><code>get_auth_header(force_refresh_token: bool = False) -&gt; dict\n</code></pre> <p>Return the <code>Authorization</code> header dict for the current access token.</p> <p>Convenience wrapper around <code>get_access_token()</code> that formats the token as a ready-to-merge header dict.</p> <p>Parameters:</p> Name Type Description Default <code>force_refresh_token</code> <code>bool</code> <p>Passed through to <code>get_access_token()</code>. When <code>True</code>, a fresh token is fetched from the IdP before building the header.</p> <code>False</code> <p>Returns:</p> Type Description <code>dict</code> <p>A dict with a single key <code>\"authorization\"</code> whose value is</p> <code>dict</code> <p><code>\"&lt;token_type&gt; &lt;access_token&gt;\"</code> (e.g.</p> <code>dict</code> <p><code>\"Bearer eyJ...\"</code>) suitable for merging into an</p> <code>dict</code> <p><code>httpx</code> request's <code>headers</code>.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If token retrieval fails.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_connections","title":"get_connections  <code>async</code>","text":"<pre><code>get_connections(params: dict = None) -&gt; List[Connection]\n</code></pre> <p>List Connections accessible to the authenticated principal.</p> <p>Returns the first page of results only (the Gundi API default page size is 20). For paginated walking, see the Pagination recipe in the docs.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>Optional dict of query parameters passed through to the API. Common keys: <code>status</code> (<code>healthy</code>, <code>unhealthy</code>, <code>disabled</code>), <code>owner</code> (organization ID), <code>search</code> (substring match).</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Connection]</code> <p>List of <code>Connection</code> objects.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_connection_details","title":"get_connection_details  <code>async</code>","text":"<pre><code>get_connection_details(integration_id: str | UUID) -&gt; Connection\n</code></pre> <p>Retrieve full details for a single Connection.</p> <p>Parameters:</p> Name Type Description Default <code>integration_id</code> <code>str | UUID</code> <p>UUID of the Connection (integration) to look up.</p> required <p>Returns:</p> Type Description <code>Connection</code> <p>A <code>Connection</code> object with all fields populated.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response (e.g. 404 if the connection does not exist or is not accessible).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_routes","title":"get_routes  <code>async</code>","text":"<pre><code>get_routes(params: dict = None) -&gt; List[Route]\n</code></pre> <p>List Routes accessible to the authenticated principal.</p> <p>Returns the first page of results only (Gundi API default page size is 20). To filter by provider connection, prefer the convenience method <code>get_routes_for_connection()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>Optional dict of query parameters. Common keys: <code>provider</code> (Connection UUID), <code>destination</code> (Connection UUID), <code>owner</code> (organization ID), <code>search</code> (substring match).</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Route]</code> <p>List of <code>Route</code> objects.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_routes_for_connection","title":"get_routes_for_connection  <code>async</code>","text":"<pre><code>get_routes_for_connection(connection_id: str | UUID) -&gt; List[Route]\n</code></pre> <p>List Routes where the given Connection appears as a data provider.</p> <p>Convenience wrapper around <code>get_routes(params={\"provider\": ...})</code>. To combine the provider filter with other server-side filters (e.g. <code>owner</code>, <code>destination</code>), call <code>get_routes()</code> directly with a merged params dict.</p> <p>Like <code>get_routes</code>, this returns only the first page of results. For paginated walking see the Pagination recipe in the docs.</p> <p>Parameters:</p> Name Type Description Default <code>connection_id</code> <code>str | UUID</code> <p>UUID (or stringifiable ID) of the Connection to filter by.</p> required <p>Returns:</p> Type Description <code>List[Route]</code> <p>List of <code>Route</code> objects where <code>connection_id</code> is a provider.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_route_details","title":"get_route_details  <code>async</code>","text":"<pre><code>get_route_details(route_id: str | UUID) -&gt; Route\n</code></pre> <p>Retrieve full details for a single Route.</p> <p>Parameters:</p> Name Type Description Default <code>route_id</code> <code>str | UUID</code> <p>UUID of the Route to look up.</p> required <p>Returns:</p> Type Description <code>Route</code> <p>A <code>Route</code> object with all fields populated.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response (e.g. 404 if the route does not exist or is not accessible).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.create_route","title":"create_route  <code>async</code>","text":"<pre><code>create_route(data: dict) -&gt; Route\n</code></pre> <p>Create a new Route via HTTP POST.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Dict describing the new route. Expected keys:</p> <ul> <li><code>name</code> (str, required): Human-readable route name.</li> <li><code>owner</code> (str, required): Organization UUID that owns   the route.</li> <li><code>data_providers</code> (list of str, required): UUIDs of   Connection objects that act as data sources.</li> <li><code>destinations</code> (list of str, required): UUIDs of   Connection objects that receive the data.</li> <li><code>configuration</code> (dict, optional): Route-level   configuration overrides.</li> </ul> required <p>Returns:</p> Type Description <code>Route</code> <p>The newly created <code>Route</code> object as returned by the API.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response (e.g. 400 for validation errors, 403 for insufficient permissions).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.update_route","title":"update_route  <code>async</code>","text":"<pre><code>update_route(route_id: str | UUID, data: dict) -&gt; Route\n</code></pre> <p>Partially update a Route via HTTP PATCH.</p> <p>Only the fields present in <code>data</code> are modified; omitted fields retain their current values on the server (standard PATCH semantics).</p> <p>Parameters:</p> Name Type Description Default <code>route_id</code> <code>str | UUID</code> <p>UUID of the Route to update.</p> required <code>data</code> <code>dict</code> <p>Partial dict of fields to update. Any subset of the keys accepted by <code>create_route()</code> is valid.</p> required <p>Returns:</p> Type Description <code>Route</code> <p>The updated <code>Route</code> object as returned by the API.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.delete_route","title":"delete_route  <code>async</code>","text":"<pre><code>delete_route(route_id: str | UUID) -&gt; None\n</code></pre> <p>Delete a Route via HTTP DELETE.</p> <p>Parameters:</p> Name Type Description Default <code>route_id</code> <code>str | UUID</code> <p>UUID of the Route to delete.</p> required <p>Returns:</p> Type Description <code>None</code> <p><code>None</code>. The Gundi API returns HTTP 204 No Content on</p> <code>None</code> <p>success.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response (e.g. 404 if the route does not exist, 403 for insufficient permissions).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_integrations","title":"get_integrations  <code>async</code>","text":"<pre><code>get_integrations(params: dict = None) -&gt; AsyncGenerator[Integration, None]\n</code></pre> <p>Iterate over all Integrations, walking pagination automatically.</p> <p>Unlike <code>get_connections()</code> and <code>get_routes()</code> which return the first page only, this method is an async generator that follows the <code>next</code> cursor returned by the API until all pages are exhausted.</p> <p>Usage::</p> <pre><code>async for integration in client.get_integrations():\n    print(integration.id)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>Optional dict of query parameters for the first request. Common keys: <code>type</code> (integration type UUID, not the slug \u2014 resolve a slug via <code>get_integration_types</code>), <code>owner</code> (organization ID), <code>search</code> (substring match). Subsequent page requests use the cursor embedded in the <code>next</code> URL and ignore this dict.</p> <code>None</code> <p>Yields:</p> Type Description <code>AsyncGenerator[Integration, None]</code> <p><code>Integration</code> objects, one per integration, across all</p> <code>AsyncGenerator[Integration, None]</code> <p>pages.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response on any page request.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_integration_types","title":"get_integration_types  <code>async</code>","text":"<pre><code>get_integration_types(params: dict = None) -&gt; AsyncGenerator[IntegrationType, None]\n</code></pre> <p>Iterate over all IntegrationTypes, walking pagination automatically.</p> <p>Mirrors <code>get_integrations</code>: an async generator that follows the <code>next</code> cursor until all pages are exhausted. Useful for resolving an integration type slug (<code>IntegrationType.value</code>) to its <code>id</code> for the <code>type</code> filter on <code>get_integrations</code>.</p> <p>Usage::</p> <pre><code>async for itype in client.get_integration_types():\n    print(itype.value, itype.id)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>Optional dict of query parameters for the first request. Subsequent page requests use the cursor embedded in the <code>next</code> URL and ignore this dict.</p> <code>None</code> <p>Yields:</p> Type Description <code>AsyncGenerator[IntegrationType, None]</code> <p><code>IntegrationType</code> objects across all pages.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response on any page request.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_activity_logs","title":"get_activity_logs  <code>async</code>","text":"<pre><code>get_activity_logs(params: dict = None) -&gt; AsyncGenerator[dict, None]\n</code></pre> <p>Iterate over activity logs, walking pagination automatically.</p> <p>Logs are returned newest-first (the API orders by <code>-created_at</code>). <code>gundi_core</code> has no ActivityLog schema, so each entry is yielded as a raw <code>dict</code> rather than a parsed model.</p> <p>Usage::</p> <pre><code>async for log in client.get_activity_logs(params={\"integration\": id}):\n    print(log[\"created_at\"], log[\"value\"])\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>Optional dict of query parameters for the first request. Common keys: <code>integration</code> (integration id), <code>integration__in</code> (comma-separated ids), <code>log_level</code>, <code>log_type</code>, <code>from_date</code>, <code>to_date</code>. Subsequent page requests use the cursor embedded in the <code>next</code> URL and ignore this dict.</p> <code>None</code> <p>Yields:</p> Type Description <code>AsyncGenerator[dict, None]</code> <p><code>dict</code> log entries across all pages, newest first.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response on any page request.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_integration_details","title":"get_integration_details  <code>async</code>","text":"<pre><code>get_integration_details(integration_id: str | UUID) -&gt; Integration\n</code></pre> <p>Retrieve full details for a single Integration.</p> <p>Parameters:</p> Name Type Description Default <code>integration_id</code> <code>str | UUID</code> <p>UUID of the Integration to look up.</p> required <p>Returns:</p> Type Description <code>Integration</code> <p>An <code>Integration</code> object with all fields populated.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response (e.g. 404 if the integration does not exist or is not accessible).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.update_integration","title":"update_integration  <code>async</code>","text":"<pre><code>update_integration(integration_id: str | UUID, data: dict) -&gt; Integration\n</code></pre> <p>Partially update an Integration via HTTP PATCH.</p> <p>Only the fields present in <code>data</code> are modified; omitted fields retain their current values on the server (standard PATCH semantics).</p> <p>To update an action configuration, pass a <code>configurations</code> list whose entries carry the configuration <code>id</code> and the new <code>data</code>::</p> <pre><code>await client.update_integration(\n    integration_id,\n    {\"configurations\": [{\"id\": config_id, \"data\": {...}}]},\n)\n</code></pre> <p>See <code>update_integration_configuration()</code> for a convenience wrapper around that common case.</p> <p>Parameters:</p> Name Type Description Default <code>integration_id</code> <code>str | UUID</code> <p>UUID of the Integration to update.</p> required <code>data</code> <code>dict</code> <p>Partial dict of fields to update.</p> required <p>Returns:</p> Type Description <code>Integration</code> <p>The updated <code>Integration</code>, re-fetched via</p> <code>Integration</code> <p><code>get_integration_details</code>. (The PATCH response itself is</p> <code>Integration</code> <p>serialized with the write serializer, which renders <code>type</code> /</p> <code>Integration</code> <p><code>owner</code> / action references as bare ids rather than the nested</p> <code>Integration</code> <p>objects the <code>Integration</code> schema requires \u2014 so we re-read the</p> <code>Integration</code> <p>canonical representation instead of parsing the PATCH body.)</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.update_integration_configuration","title":"update_integration_configuration  <code>async</code>","text":"<pre><code>update_integration_configuration(integration_id: str | UUID, configuration_id: str | UUID, data: dict) -&gt; Integration\n</code></pre> <p>Update a single action configuration's <code>data</code> on an Integration.</p> <p>Convenience wrapper around <code>update_integration()</code> for the common case of rewriting one action configuration's payload (e.g. updating a destination's field/tag mappings). The configuration is matched by its <code>id</code>; its action binding is left unchanged.</p> <p>Parameters:</p> Name Type Description Default <code>integration_id</code> <code>str | UUID</code> <p>UUID of the Integration that owns the configuration.</p> required <code>configuration_id</code> <code>str | UUID</code> <p>UUID of the action configuration to update.</p> required <code>data</code> <code>dict</code> <p>The new <code>data</code> payload for that configuration.</p> required <p>Returns:</p> Type Description <code>Integration</code> <p>The updated <code>Integration</code> object as returned by the API.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_integration_api_key","title":"get_integration_api_key  <code>async</code>","text":"<pre><code>get_integration_api_key(integration_id: str | UUID) -&gt; Optional[str]\n</code></pre> <p>Return the API key string for an Integration.</p> <p>This is the key passed as <code>integration_api_key</code> to <code>GundiDataSenderClient</code>. Note: this method returns the plain string value of the key, not a dict or object \u2014 a common point of confusion.</p> <p>Parameters:</p> Name Type Description Default <code>integration_id</code> <code>str | UUID</code> <p>UUID of the Integration whose API key is requested.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>The API key as a plain string, or <code>None</code> if the response did</p> <code>Optional[str]</code> <p>not contain an <code>api_key</code> field. Callers should defensively</p> <code>Optional[str]</code> <p>check for <code>None</code> before passing the value to</p> <code>Optional[str]</code> <p><code>GundiDataSenderClient</code>.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response (e.g. 403 if the caller lacks permission to read the key).</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.get_traces","title":"get_traces  <code>async</code>","text":"<pre><code>get_traces(params: dict) -&gt; List[GundiTrace]\n</code></pre> <p>List Gundi data traces (first page only).</p> <p>Traces record the routing history of individual data points through the Gundi pipeline and are useful for debugging delivery issues.</p> <p>Parameters:</p> Name Type Description Default <code>params</code> <code>dict</code> <p>Dict of query parameters. Common keys: <code>object_id</code> (source record UUID), <code>integration</code> (integration UUID), <code>created_at__gte</code> / <code>created_at__lte</code> (ISO-8601 datetime bounds).</p> required <p>Returns:</p> Type Description <code>List[GundiTrace]</code> <p>List of <code>GundiTrace</code> objects from the first result page.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"api-reference/gundi-client/#gundi_client_v2.client.GundiClient.register_integration_type","title":"register_integration_type  <code>async</code>","text":"<pre><code>register_integration_type(data: dict) -&gt; IntegrationType\n</code></pre> <p>Register or update an IntegrationType in the Gundi portal.</p> <p>This is an administrative operation typically called by integration services on startup (when <code>REGISTER_ON_START=true</code>) to declare their capabilities, configuration schema, and webhook endpoints to Gundi.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Dict describing the integration type. Consult the Gundi API documentation for the full schema; commonly includes <code>name</code>, <code>description</code>, <code>type_slug</code>, <code>service_url</code>, and <code>actions</code>.</p> required <p>Returns:</p> Type Description <code>IntegrationType</code> <p>The created or updated <code>IntegrationType</code> object as returned</p> <code>IntegrationType</code> <p>by the API.</p> <p>Raises:</p> Type Description <code>AuthenticationError</code> <p>If the OAuth token request fails.</p> <code>GundiAPIError</code> <p>If the API returns a 4xx/5xx response.</p>"},{"location":"authentication/client-credentials/","title":"Client credentials grant","text":"<p>The <code>client_credentials</code> grant (RFC 6749 \u00a74.4) is the standard OAuth2 flow for server-to-server authentication. There is no user identity involved \u2014 your service authenticates as itself using a client ID and secret.</p>"},{"location":"authentication/client-credentials/#when-to-use-it","title":"When to use it","text":"<ul> <li>Your service is a confidential client (the secret can be kept private).</li> <li>The actions you perform are not tied to a specific user.</li> <li>Common use: batch jobs, schedulers, integrations that act as a system   account.</li> </ul>"},{"location":"authentication/client-credentials/#environment-variables","title":"Environment variables","text":"<pre><code>GUNDI_API_BASE_URL=https://api.gundiservice.org\nGUNDI_OAUTH_ISSUER=https://auth.gundiservice.org/realms/your-realm\nGUNDI_OAUTH_CLIENT_ID=your-client-id\nGUNDI_OAUTH_CLIENT_SECRET=your-client-secret\n# GUNDI_OAUTH_AUDIENCE=your-api-audience   # required by some IdPs (e.g. Auth0)\n</code></pre>"},{"location":"authentication/client-credentials/#minimal-example","title":"Minimal example","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        connections = await client.get_connections()\n        print(f\"{len(connections)} connections accessible to this client\")\n\n\nasyncio.run(main())\n</code></pre> <p>The client reads the env vars at construction time. The first request triggers an initial <code>client_credentials</code> token request; subsequent requests reuse the cached token until it expires.</p>"},{"location":"authentication/client-credentials/#whats-on-the-wire","title":"What's on the wire","text":"<p>The library POSTs to the token endpoint with:</p> <pre><code>grant_type=client_credentials\nclient_id=&lt;GUNDI_OAUTH_CLIENT_ID&gt;\nclient_secret=&lt;GUNDI_OAUTH_CLIENT_SECRET&gt;\nscope=openid\naudience=&lt;GUNDI_OAUTH_AUDIENCE&gt;     # only if set\n</code></pre> <p>The token endpoint is either <code>GUNDI_OAUTH_TOKEN_URL</code> directly, or resolved via OIDC discovery from <code>GUNDI_OAUTH_ISSUER</code>.</p>"},{"location":"authentication/client-credentials/#refresh-behavior","title":"Refresh behavior","text":"<p>Per RFC 6749 \u00a74.4.3, the server SHOULD NOT return a refresh token for a <code>client_credentials</code> response. The library handles this:</p> <ul> <li>If the response includes a refresh token, normal refresh-token rotation   applies.</li> <li>If the response omits a refresh token (the common case), the library marks   the token as \"no refresh available\" internally and re-runs   <code>client_credentials</code> on the next expiry. No user credentials are involved,   so re-authenticating is cheap.</li> </ul>"},{"location":"authentication/client-credentials/#constructing-the-client-explicitly","title":"Constructing the client explicitly","text":"<p>If you'd rather not use env vars, pass the values as kwargs:</p> <pre><code>client = GundiClient(\n    base_url=\"https://api.gundiservice.org\",\n    oauth_issuer=\"https://auth.gundiservice.org/realms/your-realm\",\n    oauth_client_id=\"your-client-id\",\n    oauth_client_secret=\"your-client-secret\",\n    # oauth_audience=\"your-api-audience\",\n)\n</code></pre> <p>The kwargs win over env vars.</p>"},{"location":"authentication/client-credentials/#see-also","title":"See also","text":"<ul> <li>Password grant for user-facing flows</li> <li>OIDC discovery for the issuer-based token endpoint resolution</li> <li>Refresh and errors for token-refresh internals</li> </ul>"},{"location":"authentication/oidc-discovery/","title":"OIDC discovery","text":"<p>OIDC Discovery 1.0 specifies that any OIDC-compliant IdP MUST expose a metadata document at <code>{issuer}/.well-known/openid-configuration</code>. That document includes the <code>token_endpoint</code>, <code>authorization_endpoint</code>, supported scopes, and other configuration.</p> <p><code>gundi-client-v2</code> uses this to avoid hard-coding the token endpoint URL. Set <code>GUNDI_OAUTH_ISSUER</code> to the IdP's issuer base URL, and the library fetches and caches the resolved token endpoint at runtime.</p>"},{"location":"authentication/oidc-discovery/#when-to-use-it","title":"When to use it","text":"<ul> <li>You don't want to look up and hard-code the exact token endpoint URL.</li> <li>You're rolling the same code out against multiple IdPs (Keycloak,   Auth0, Okta, Azure AD).</li> <li>You want to be resilient to the IdP team changing the token endpoint   path (rare, but possible).</li> </ul>"},{"location":"authentication/oidc-discovery/#environment-variable","title":"Environment variable","text":"<pre><code>GUNDI_OAUTH_ISSUER=https://auth.gundiservice.org/realms/your-realm\n</code></pre> <p>Combine with the credentials for your chosen grant (client_credentials or password).</p>"},{"location":"authentication/oidc-discovery/#what-happens-at-runtime","title":"What happens at runtime","text":"<ol> <li>The first authentication attempt calls    <code>auth.discover_token_endpoint(session, issuer)</code>.</li> <li>The function GETs <code>{issuer.rstrip('/')}/.well-known/openid-configuration</code>.</li> <li>It validates that the document's <code>issuer</code> claim matches the URL used to    fetch it (OIDC Discovery 1.0 \u00a74.3 \u2014 protects against    misconfiguration and man-in-the-middle).</li> <li>The resolved <code>token_endpoint</code> is cached in a module-level dict keyed by    the normalized issuer.</li> <li>Subsequent authentication attempts reuse the cached value.</li> </ol> <p>The cache lives for the process lifetime. Long-running services that need to pick up an IdP configuration change without restarting can call:</p> <pre><code>from gundi_client_v2.auth import clear_discovery_cache\n\nclear_discovery_cache()\n</code></pre>"},{"location":"authentication/oidc-discovery/#performance-note","title":"Performance note","text":"<p>OIDC discovery adds one extra HTTP request to the very first authentication. Subsequent requests in the same process do no additional network I/O for discovery.</p>"},{"location":"authentication/oidc-discovery/#skipping-discovery","title":"Skipping discovery","text":"<p>If your IdP doesn't expose a discovery document, or you want to skip the extra request, set <code>GUNDI_OAUTH_TOKEN_URL</code> directly instead. When both are set, <code>GUNDI_OAUTH_TOKEN_URL</code> wins:</p> <pre><code>GUNDI_OAUTH_TOKEN_URL=https://auth.example.com/realms/myrealm/protocol/openid-connect/token\n</code></pre>"},{"location":"authentication/oidc-discovery/#errors","title":"Errors","text":"Error Meaning <code>OIDC discovery failed for ...</code> Network error or non-2xx response fetching the discovery document. Check connectivity and that the issuer URL is correct. <code>OIDC discovery document at ... is not valid JSON</code> The discovery endpoint returned something that isn't JSON. Check that the URL is correct and not returning an HTML error page. <code>OIDC discovery document at ... is not a JSON object</code> The endpoint returned JSON, but not an object. Almost certainly an IdP bug. <code>OIDC discovery document at ... returned issuer ... which does not match the expected issuer ...</code> The <code>issuer</code> claim in the discovery doc doesn't match the URL used to fetch it. Common cause: trailing slash mismatch (the library normalizes both via <code>rstrip('/')</code>, so this shouldn't trip you up), or IdP misconfiguration. <code>OIDC discovery document at ... is missing 'token_endpoint'</code> The IdP exposed a discovery document but didn't include the required <code>token_endpoint</code> field. Almost certainly an IdP bug."},{"location":"authentication/oidc-discovery/#see-also","title":"See also","text":"<ul> <li>Refresh and errors for general auth-error handling</li> <li>The full example: <code>examples/list_connections_discovery.py</code></li> </ul>"},{"location":"authentication/overview/","title":"Authentication overview","text":"<p><code>gundi-client-v2</code> supports three OAuth2 authentication paths. All three end the same way \u2014 the client holds an access token and attaches it to every outgoing request \u2014 but they differ in how that token is obtained.</p>"},{"location":"authentication/overview/#the-three-paths","title":"The three paths","text":"Path Grant When Client type Client credentials <code>grant_type=client_credentials</code> (RFC 6749 \u00a74.4) Server-to-server, no user identity Confidential (has a secret) Password grant <code>grant_type=password</code> (ROPC; RFC 6749 \u00a74.3) Acting on behalf of a specific user Public (no secret) OIDC discovery Either of the above, with token endpoint discovered automatically Any IdP that supports OIDC Discovery 1.0 Either <p>OIDC discovery is not a separate grant type \u2014 it's a mechanism for the library to discover the IdP's token endpoint at runtime from <code>{GUNDI_OAUTH_ISSUER}/.well-known/openid-configuration</code>. You combine it with one of the other two grants.</p> <p>The un-prefixed <code>OAUTH_*</code> names (and, for the library, the legacy <code>KEYCLOAK_*</code> names) are still accepted as fallbacks.</p>"},{"location":"authentication/overview/#how-the-client-picks-a-grant","title":"How the client picks a grant","text":"<p>When you call any method on <code>GundiClient</code> that requires authentication, it chooses a grant in this priority order:</p> <ol> <li>If a non-expired refresh token is cached \u2192 refresh-token grant    (RFC 6749 \u00a76).</li> <li>If <code>username</code> + <code>password</code> were provided \u2192 password grant.</li> <li>If <code>client_secret</code> was provided \u2192 client_credentials grant.</li> <li>Otherwise \u2192 <code>AuthenticationError</code>.</li> </ol> <p>You don't normally call the grant functions directly; the client handles it. But the standalone functions in <code>gundi_client_v2.auth</code> are available if you need fine-grained control:</p> <pre><code>from gundi_client_v2.auth import (\n    get_access_token_client_credentials,\n    get_access_token_password_grant,\n    refresh_access_token,\n    discover_token_endpoint,\n)\n</code></pre>"},{"location":"authentication/overview/#resolving-the-token-endpoint","title":"Resolving the token endpoint","text":"<p>The library needs to know where to POST the token request. Two ways to configure this, in priority order:</p> <ol> <li><code>GUNDI_OAUTH_TOKEN_URL</code> (or the <code>oauth_token_url</code> kwarg) \u2014 set this    directly to a fully-qualified URL like    <code>https://auth.example.com/realms/myrealm/protocol/openid-connect/token</code>.</li> <li><code>GUNDI_OAUTH_ISSUER</code> (or the <code>oauth_issuer</code> kwarg) \u2014 set this to the    issuer base URL; the library performs OIDC discovery to resolve the token    endpoint. The result is cached for the process lifetime; call    <code>auth.clear_discovery_cache()</code> to invalidate.</li> </ol> <p>If neither is set, the client raises <code>AuthenticationError</code>.</p>"},{"location":"authentication/overview/#errors-you-might-see","title":"Errors you might see","text":"Error Likely cause <code>AuthenticationError: Token request failed: HTTP 401 (invalid_client)</code> Wrong client ID or secret <code>AuthenticationError: Token request failed: HTTP 401 (invalid_grant)</code> Wrong username/password, or a revoked refresh token <code>AuthenticationError: OIDC discovery failed for ...</code> Issuer URL unreachable or doesn't expose <code>.well-known/openid-configuration</code> <code>AuthenticationError: ...returned issuer ... does not match the expected issuer</code> The discovery doc's <code>issuer</code> claim doesn't match the URL you used to fetch it \u2014 possible misconfiguration or man-in-the-middle <p>Full details on each, with mitigation steps: Refresh and errors.</p>"},{"location":"authentication/overview/#see-also","title":"See also","text":"<ul> <li>Client credentials \u2014 confidential clients, M2M flows</li> <li>Password grant \u2014 user-facing flows</li> <li>OIDC discovery \u2014 IdP-agnostic token endpoint resolution</li> <li>Refresh and errors \u2014 refresh token rotation, error scenarios</li> </ul>"},{"location":"authentication/password-grant/","title":"Password grant","text":"<p>The <code>password</code> grant (Resource Owner Password Credentials, ROPC; RFC 6749 \u00a74.3) authenticates a user by sending their username and password directly to the IdP.</p> <p>ROPC is discouraged by current OAuth best practice</p> <p>RFC 9700 (\"OAuth 2.0 Security Best Current Practice\", Dec 2024) discourages ROPC in favor of authorization-code flows with PKCE; the in-progress OAuth 2.1 draft removes it entirely. <code>gundi-client-v2</code> still supports it for legacy and CLI use cases where redirect-based flows aren't practical.</p>"},{"location":"authentication/password-grant/#when-to-use-it","title":"When to use it","text":"<ul> <li>Your code runs in a context where you have the user's credentials   (a CLI tool, a desktop utility, an internal admin script).</li> <li>Your OAuth client is public (no client secret).</li> <li>You need to act as the user, not as a system account.</li> </ul>"},{"location":"authentication/password-grant/#environment-variables","title":"Environment variables","text":"<pre><code>GUNDI_API_BASE_URL=https://api.gundiservice.org\nGUNDI_OAUTH_ISSUER=https://auth.gundiservice.org/realms/your-realm\nGUNDI_OAUTH_CLIENT_ID=your-client-id\nGUNDI_USERNAME=your-username\nGUNDI_PASSWORD=your-password\n# GUNDI_OAUTH_AUDIENCE=your-api-audience   # required by some IdPs (e.g. Auth0)\n</code></pre>"},{"location":"authentication/password-grant/#minimal-example","title":"Minimal example","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        integrations = []\n        async for integration in client.get_integrations():\n            integrations.append(integration)\n        print(f\"User has access to {len(integrations)} integrations\")\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"authentication/password-grant/#whats-on-the-wire","title":"What's on the wire","text":"<pre><code>grant_type=password\nclient_id=&lt;GUNDI_OAUTH_CLIENT_ID&gt;\nusername=&lt;GUNDI_USERNAME&gt;\npassword=&lt;GUNDI_PASSWORD&gt;\nscope=openid\naudience=&lt;GUNDI_OAUTH_AUDIENCE&gt;     # only if set\n</code></pre> <p>No <code>client_secret</code> \u2014 public clients don't have one.</p>"},{"location":"authentication/password-grant/#refresh-behavior","title":"Refresh behavior","text":"<p>A successful password-grant response typically includes a <code>refresh_token</code>. The library caches it and uses it on the next expiry (RFC 6749 \u00a76) to avoid re-sending the user's credentials.</p> <p>If the server rotates the refresh token on each refresh (default in Keycloak), the new one replaces the cached one. If the server omits a new refresh token in the refresh response (also allowed by \u00a76), the library reuses the existing cached refresh token until it too expires.</p> <p>When the refresh token itself expires, the library falls back to a fresh password grant \u2014 provided <code>GUNDI_USERNAME</code> and <code>GUNDI_PASSWORD</code> are still in the env. If they've been unset, you'll see an <code>AuthenticationError</code>.</p>"},{"location":"authentication/password-grant/#constructing-the-client-explicitly","title":"Constructing the client explicitly","text":"<pre><code>client = GundiClient(\n    base_url=\"https://api.gundiservice.org\",\n    oauth_issuer=\"https://auth.gundiservice.org/realms/your-realm\",\n    oauth_client_id=\"your-client-id\",\n    username=\"your-username\",\n    password=\"your-password\",\n)\n</code></pre>"},{"location":"authentication/password-grant/#see-also","title":"See also","text":"<ul> <li>Client credentials for confidential M2M clients</li> <li>OIDC discovery for issuer-based token endpoint resolution</li> <li>Refresh and errors for refresh-rotation details</li> </ul>"},{"location":"authentication/refresh-and-errors/","title":"Refresh and errors","text":"<p>How the library reuses tokens, when it re-authenticates, and the errors you'll see when something goes wrong.</p>"},{"location":"authentication/refresh-and-errors/#token-lifecycle-inside-gundiclient","title":"Token lifecycle inside <code>GundiClient</code>","text":"<p>The client caches the most recent <code>OAuthToken</code> in memory, along with two computed timestamps:</p> <ul> <li><code>expires_at</code> \u2014 when the access token will become invalid</li> <li><code>refresh_expires_at</code> \u2014 when the refresh token will become invalid</li> </ul> <p>Both have a small safety buffer (15 seconds) subtracted so the client treats a token as expired slightly before the server does.</p> <p>On every authenticated request, the client checks:</p> <ol> <li>Is the access token still valid? \u2192 Use it.</li> <li>Is the refresh token still valid? \u2192 Refresh and use the new access    token (RFC 6749 \u00a76).</li> <li>Otherwise \u2192 Re-authenticate via the configured grant.</li> </ol>"},{"location":"authentication/refresh-and-errors/#refresh-token-rotation","title":"Refresh token rotation","text":"<p>RFC 6749 \u00a76 makes returning a new refresh token in the refresh response optional. Two scenarios:</p> <p>Rotating IdP (Keycloak default, most modern IdPs): every refresh response includes a new <code>refresh_token</code> that replaces the previous one. The client treats this as the canonical behavior and updates its cache.</p> <p>Non-rotating IdP: the refresh response includes only a new access token, no new refresh token. The client recognizes this case, keeps the existing cached refresh token, and continues using it until the IdP rejects it.</p> <p>You don't have to do anything to support either case \u2014 the library detects which one your IdP is using.</p>"},{"location":"authentication/refresh-and-errors/#client_credentials-re-authentication","title":"<code>client_credentials</code> re-authentication","text":"<p><code>client_credentials</code> responses don't usually include a refresh token (per RFC 6749 \u00a74.4.3). The client treats these tokens as \"no refresh available\": when they expire, it runs the <code>client_credentials</code> grant again. Because no user credentials are involved, this is cheap.</p>"},{"location":"authentication/refresh-and-errors/#common-errors","title":"Common errors","text":""},{"location":"authentication/refresh-and-errors/#token-request-failed-http-401-invalid_client","title":"<code>Token request failed: HTTP 401 (invalid_client)</code>","text":"<p>The client ID or secret is wrong, or the client doesn't exist in the realm.</p> <p>Fix: Verify <code>GUNDI_OAUTH_CLIENT_ID</code> and <code>GUNDI_OAUTH_CLIENT_SECRET</code>. For Keycloak, also confirm the client is in the correct realm under <code>GUNDI_OAUTH_ISSUER</code>.</p>"},{"location":"authentication/refresh-and-errors/#token-request-failed-http-401-invalid_grant","title":"<code>Token request failed: HTTP 401 (invalid_grant)</code>","text":"<p>The credentials don't match a user (password grant), or the refresh token has been revoked or expired (refresh grant).</p> <p>Fix: For password grant, verify <code>GUNDI_USERNAME</code> and <code>GUNDI_PASSWORD</code>. For refresh grant, the client should fall back to a fresh password/client_ credentials grant automatically; if it doesn't, check that the fallback credentials are still in the env.</p>"},{"location":"authentication/refresh-and-errors/#token-request-failed-http-401-unauthorized_client","title":"<code>Token request failed: HTTP 401 (unauthorized_client)</code>","text":"<p>The client isn't permitted to use this grant type at the IdP. Common in Keycloak when \"Direct Access Grants Enabled\" (password grant) or \"Service Accounts Enabled\" (client_credentials) hasn't been toggled on for the client.</p> <p>Fix: Ask your IdP administrator to enable the appropriate grant for the client.</p>"},{"location":"authentication/refresh-and-errors/#token-request-failed-http-400-invalid_scope","title":"<code>Token request failed: HTTP 400 (invalid_scope)</code>","text":"<p>The requested scope isn't allowed for this client.</p> <p>Fix: Pass a <code>scope=...</code> kwarg to the grant function, or unset <code>GUNDI_OAUTH_SCOPE</code> if you set it explicitly. The library's default scope is <code>openid</code>.</p>"},{"location":"authentication/refresh-and-errors/#authenticationerror-oidc-discovery-failed-for","title":"<code>AuthenticationError: OIDC discovery failed for ...</code>","text":"<p>See OIDC discovery \u2192 Errors.</p>"},{"location":"authentication/refresh-and-errors/#handling-auth-errors-in-your-code","title":"Handling auth errors in your code","text":"<p><code>AuthenticationError</code> is a subclass of <code>gundi_client_v2.errors.GundiClientError</code>. Catch it around your client calls if you want to handle expired credentials gracefully:</p> <pre><code>from gundi_client_v2 import GundiClient\nfrom gundi_client_v2.errors import AuthenticationError\n\n\nasync def main():\n    try:\n        async with GundiClient() as client:\n            connections = await client.get_connections()\n    except AuthenticationError as e:\n        print(f\"Auth failed: {e}\")\n        # ... fetch new credentials, prompt user, exit, etc.\n</code></pre>"},{"location":"authentication/refresh-and-errors/#see-also","title":"See also","text":"<ul> <li>Overview for the grant-selection logic</li> <li>Troubleshooting for non-auth errors</li> </ul>"},{"location":"concepts/data-model/","title":"Gundi data model","text":"<p>Gundi sits between data providers (devices, third-party APIs, telemetry sources) and destinations (EarthRanger sites, SMART deployments, etc.). Five resource types make up the platform:</p> <pre><code>Integration \u2500\u2500\u25ba Connection \u2500\u2500\u25ba Route \u2500\u2500\u25ba Integration (destination)\n                                    \u2514\u2500\u2500\u25ba Integration (destination)\n</code></pre>"},{"location":"concepts/data-model/#integration","title":"Integration","text":"<p>A registered third-party system that either provides data to Gundi or receives data from it. Defined by an <code>IntegrationType</code> (e.g. <code>er_traptagger</code>, <code>lotek</code>, <code>earthranger</code>) and an <code>IntegrationConfiguration</code> that holds the system-specific settings (base URL, API key, etc.).</p> <p>You read these with: <code>get_integrations()</code>, <code>get_integration_details(integration_id)</code>.</p>"},{"location":"concepts/data-model/#connection","title":"Connection","text":"<p>A pairing of a provider Integration with one or more destination Integrations. A \"TrapTagger camera trap deployment at Park X\" might be a Connection whose provider is the TrapTagger Integration and whose destinations are the EarthRanger site(s) receiving its events.</p> <p>A Connection's primary fields:</p> <ul> <li><code>provider</code> \u2014 the Integration that produces data</li> <li><code>destinations</code> \u2014 list of Integrations that receive routed data</li> <li><code>status</code> \u2014 <code>healthy</code>, <code>unhealthy</code>, <code>disabled</code></li> <li><code>routing_rules</code> \u2014 references to the Routes that direct the data</li> <li><code>default_route</code> \u2014 the Route used when no other rule matches (optional)</li> </ul> <p>You read these with: <code>get_connections()</code>, <code>get_connection_details(integration_id)</code>.</p>"},{"location":"concepts/data-model/#route","title":"Route","text":"<p>A rule that maps provider Integrations to destination Integrations, with optional field-level transforms (often a JQ filter). Routes belong to an owner Organization. A Connection's <code>routing_rules</code> are the Route IDs that apply to it.</p> <p>You read these with: <code>get_routes()</code>, <code>get_route_details(route_id)</code>, <code>get_routes_for_connection(connection_id)</code>. You manage these with: <code>create_route()</code>, <code>update_route()</code>, <code>delete_route()</code>.</p>"},{"location":"concepts/data-model/#observation-event-message","title":"Observation, Event, Message","text":"<p>The three payload types Gundi forwards:</p> Type What it is Sent with Observation A geolocated point (lat, lon, time, optional attributes) <code>GundiDataSenderClient.post_observations</code> Event A discrete incident or record (type, time, location, attributes) <code>GundiDataSenderClient.post_events</code> Message A text message (often paired with an Event or Observation) <code>GundiDataSenderClient.post_messages</code>"},{"location":"concepts/data-model/#trace","title":"Trace","text":"<p>A record of one payload's journey through Gundi: the source observation/event, which Connection routed it, which destination(s) received it, and the delivery outcome. Useful for diagnostics.</p> <p>You read these with: <code>get_traces(params={...})</code>.</p>"},{"location":"concepts/data-model/#how-they-fit-together","title":"How they fit together","text":"<p>A typical \"provider \u2192 destination\" flow:</p> <ol> <li>A device pushes an Observation to Gundi via a provider Integration's    webhook endpoint.</li> <li>Gundi looks up the Connections containing that provider Integration.</li> <li>For each matching Connection, Gundi consults its Routes to determine    which destination Integrations should receive the data.</li> <li>Gundi transforms the payload per the Route's <code>transformation_rules</code> if    any, then forwards it to each destination.</li> <li>A Trace is recorded for each provider \u2192 destination edge in the flow.</li> </ol>"},{"location":"concepts/payload-types/","title":"Payload types","text":""},{"location":"concepts/payload-types/#three-payload-types","title":"Three payload types","text":"<p>Gundi forwards three payload types: Observations, Events, and Messages. Each maps to a different kind of real-world datum and a different <code>GundiDataSenderClient</code> method. The side-by-side table at the bottom of this page summarises the key differences at a glance.</p>"},{"location":"concepts/payload-types/#observation","title":"Observation","text":"<p>An Observation is a geolocated, time-stamped data point produced automatically by a sensor or telemetry device \u2014 a GPS collar fix, a camera-trap detection, a stationary environmental sensor reading. Observations are the most common payload type in Gundi integrations.</p> <p>Common fields:</p> Field Notes <code>source</code> Identifier for the device or feed that produced the point <code>type</code> Payload sub-type; common values: <code>tracking-device</code>, <code>stationary-object</code>, <code>gps-radio-collar</code> <code>recorded_at</code> ISO-8601 timestamp when the device recorded the reading <code>location.lat</code> / <code>location.lon</code> WGS-84 decimal degrees (both required) <code>additional</code> Free-form dict for any extra sensor attributes <p>Observations are always sent in batches (<code>List[dict]</code>), even when the batch contains a single item.</p> <p>See also: Sending Observations</p>"},{"location":"concepts/payload-types/#event","title":"Event","text":"<p>An Event is a discrete, operator-meaningful incident \u2014 a ranger sighting, a poaching incident report, a system alarm. Events are typically created by a human (or a rule engine acting on human-defined logic) rather than raw telemetry.</p> <p>Common fields:</p> Field Notes <code>event_type</code> Domain-specific type string (e.g. <code>wildlife_sighting</code>, <code>fence_alarm</code>) <code>recorded_at</code> ISO-8601 timestamp when the incident occurred <code>location.lat</code> / <code>location.lon</code> WGS-84 decimal degrees (both required) <code>event_details</code> Dict of event-specific structured attributes <code>title</code> Short human-readable description <p>Like Observations, Events are sent in batches.</p> <p>See also: Sending Events</p>"},{"location":"concepts/payload-types/#message","title":"Message","text":"<p>A Message is a text annotation \u2014 a comment, a dispatch note, a status update. Messages are often attached to an Event to add context that doesn't fit neatly into structured fields, but they can also stand alone.</p> <p>Common fields:</p> <p>Minimum fields:</p> Field Notes <code>sender</code> Identifier of the sending party <code>recipients</code> List of recipient identifiers (users, groups, or channels) <code>text</code> The message body <p>Optional fields:</p> Field Notes <code>event_id</code> Links this message to an existing Event <code>recorded_at</code> ISO-8601 timestamp when the message was created <code>location.latitude</code> / <code>location.longitude</code> WGS-84 decimal degrees. Note: Messages use the long form <code>latitude</code>/<code>longitude</code>, unlike Observations and Events which use <code>lat</code>/<code>lon</code>. <p>Messages are sent in batches.</p> <p>See also: Sending Messages and Attachments</p>"},{"location":"concepts/payload-types/#side-by-side-comparison","title":"Side-by-side comparison","text":"Aspect Observation Event Message Has a location Yes (required) Yes (required) Optional Has a time <code>recorded_at</code> <code>recorded_at</code> <code>recorded_at</code> (optional) Sent in batches Yes (<code>List[dict]</code>) Yes (<code>List[dict]</code>) Yes (<code>List[dict]</code>) Common source Telemetry / sensors Operator-entered incidents Comms / context"},{"location":"concepts/payload-types/#what-gets-routed-where","title":"What gets routed where","text":"<p>All three payload types flow through the same routing machinery. When Gundi receives any payload it:</p> <ol> <li>Looks up the Connections that include the provider Integration.</li> <li>Evaluates the matching Routes for each Connection.</li> <li>Applies any field-level transformation defined in the Route's <code>configuration</code>    (often a JQ filter) to the payload.</li> <li>Forwards the transformed payload to each destination Integration.</li> </ol> <p>No payload type is treated as special by the router \u2014 a Route's <code>configuration</code> applies equally to Observations, Events, and Messages. The payload type determines only which <code>GundiDataSenderClient</code> method you call on the sending side and which schema the destination expects on the receiving side.</p> <p>See also:</p> <ul> <li>Gundi data model \u2014 Integration, Connection, and   Route vocabulary</li> <li>Sending Observations</li> <li>Sending Events</li> <li>Sending Messages and Attachments</li> </ul>"},{"location":"concepts/tracing-lifecycle/","title":"Tracing and lifecycle","text":""},{"location":"concepts/tracing-lifecycle/#how-a-payload-moves-through-gundi","title":"How a payload moves through Gundi","text":"<p>Every observation, event, or message you send travels through a predictable sequence of steps before it reaches a destination system. Understanding that sequence makes it easier to diagnose problems and verify delivery.</p> <ol> <li> <p>Ingestion. A provider Integration delivers a payload to Gundi \u2014 via an    inbound webhook, a scheduled pull action, or a direct call to    <code>GundiDataSenderClient.post_observations()</code> / <code>post_events()</code> /    <code>post_messages()</code>.</p> </li> <li> <p>ID assignment. Gundi assigns the payload an internal identifier:    <code>object_id</code>. This ID is stable and is the handle you use to look up Traces    later.</p> </li> <li> <p>Connection lookup. Gundi finds every Connection whose <code>provider</code> matches    the Integration that delivered the payload.</p> </li> <li> <p>Route resolution. For each matching Connection, Gundi evaluates its    <code>routing_rules</code> to find the Routes that apply. If no rule matches, Gundi    falls back to the Connection's <code>default_route</code> (if one is configured).</p> </li> <li> <p>Transform and forward. For each (Route, destination) pair, Gundi applies    the Route's <code>configuration</code> \u2014 typically a JQ filter \u2014 to the payload, then    forwards the result to the destination Integration's API.</p> </li> <li> <p>Trace recording. Gundi writes a <code>Trace</code> for every provider \u2192 destination    edge in the flow, capturing the delivery outcome (success, error, duplicate    suppression) and the <code>external_id</code> assigned by the destination.</p> </li> </ol>"},{"location":"concepts/tracing-lifecycle/#the-trace-record","title":"The Trace record","text":"<p>Each <code>Trace</code> corresponds to one payload crossing one provider \u2192 destination edge. The fields below are the canonical set exposed by <code>gundi_core.schemas.v2.GundiTrace</code>.</p> Field Meaning <code>object_id</code> The Gundi-side ID of the source observation/event <code>object_type</code> Payload type discriminator <code>related_to</code> Parent object ID (e.g. for messages attached to an event) <code>data_provider</code> Provider Integration ID <code>destination</code> Destination Integration ID <code>delivered_at</code> When the payload reached the destination <code>external_id</code> The ID assigned by the destination (e.g. an EarthRanger event UUID) <code>created_at</code> / <code>updated_at</code> Trace timestamps <code>last_update_delivered_at</code> Time of the most recent successful update <code>is_duplicate</code> True if Gundi suppressed this as a duplicate <code>has_error</code> True if delivery errored <p>You retrieve Traces with <code>GundiClient.get_traces(params={...})</code>.</p>"},{"location":"concepts/tracing-lifecycle/#when-to-read-traces","title":"When to read traces","text":"<p>Diagnosing a missing payload. If a payload you sent never appeared at the destination, pull its Traces and check <code>has_error</code>. If <code>has_error</code> is <code>True</code>, the <code>Trace</code> record is your starting point for understanding what went wrong \u2014 check the destination Integration's configuration and the Route's transformation for the failing edge.</p> <p>Confirming delivery. <code>delivered_at</code> tells you exactly when the destination acknowledged receipt. If <code>delivered_at</code> is set and <code>has_error</code> is <code>False</code>, the payload was delivered successfully. If <code>is_duplicate</code> is <code>True</code>, Gundi suppressed it because an identical payload had already been delivered.</p> <p>Finding the destination's external ID. Gundi stores the ID the destination assigned to the forwarded payload in <code>external_id</code>. Use this when you need to correlate a Gundi <code>object_id</code> with, say, an EarthRanger event UUID or a SMART patrol ID for downstream operations.</p> <p>See also:</p> <ul> <li>Gundi data model \u2014 Integration, Connection, and   Route vocabulary</li> <li>Reading Traces</li> </ul>"},{"location":"getting-started/auth-setup/","title":"Authentication setup","text":"<p><code>gundi-client-v2</code> reads its OAuth2 configuration from environment variables by default. This page covers the happy path for most users; for detailed coverage of each grant type, see the Authentication section.</p>"},{"location":"getting-started/auth-setup/#what-you-need-from-your-gundi-administrator","title":"What you need from your Gundi administrator","text":"<ul> <li>The API base URL for your Gundi deployment (e.g.   <code>https://api.gundiservice.org</code>).</li> <li>An OAuth issuer URL for the IdP behind Gundi   (e.g. <code>https://auth.gundiservice.org/realms/your-realm</code>).</li> <li>An OAuth client (ID, and a secret if it's a confidential client).</li> <li>Either user credentials (for password grant) or a client secret   (for client_credentials).</li> </ul>"},{"location":"getting-started/auth-setup/#choose-a-grant-type","title":"Choose a grant type","text":"Use this When <code>client_credentials</code> Server-to-server, no user identity involved. Your client is confidential (has a secret). <code>password</code> Acting on behalf of a known user with their username and password. <code>GUNDI_OAUTH_CLIENT_SECRET</code> is not used by this grant. <p>If unsure, ask your Gundi administrator which grant your client is configured for.</p>"},{"location":"getting-started/auth-setup/#set-the-environment-variables","title":"Set the environment variables","text":"<p>Create a <code>.env</code> file next to your application code, or export these in your shell:</p> client_credentialspassword grant <pre><code>GUNDI_API_BASE_URL=https://api.gundiservice.org\nGUNDI_OAUTH_ISSUER=https://auth.gundiservice.org/realms/your-realm\nGUNDI_OAUTH_CLIENT_ID=your-client-id\nGUNDI_OAUTH_CLIENT_SECRET=your-client-secret\n# GUNDI_OAUTH_AUDIENCE=your-api-audience   # required by some IdPs (e.g. Auth0)\n</code></pre> <pre><code>GUNDI_API_BASE_URL=https://api.gundiservice.org\nGUNDI_OAUTH_ISSUER=https://auth.gundiservice.org/realms/your-realm\nGUNDI_OAUTH_CLIENT_ID=your-client-id\nGUNDI_USERNAME=your-username\nGUNDI_PASSWORD=your-password\n# GUNDI_OAUTH_AUDIENCE=your-api-audience   # required by some IdPs (e.g. Auth0)\n</code></pre> <p>The un-prefixed <code>OAUTH_*</code> names (and, for the library, the legacy <code>KEYCLOAK_*</code> names) are still accepted as fallbacks.</p>"},{"location":"getting-started/auth-setup/#how-the-library-uses-these","title":"How the library uses these","text":"<p>When you create a <code>GundiClient()</code> with no arguments, it reads these env vars through <code>gundi_client_v2.settings</code>. Setting <code>GUNDI_OAUTH_ISSUER</code> triggers OIDC discovery: the library fetches the IdP's <code>/.well-known/openid-configuration</code> document on the first auth attempt and caches the resolved token endpoint for the process lifetime.</p> <p>If your IdP doesn't expose discovery, set <code>GUNDI_OAUTH_TOKEN_URL</code> directly instead of <code>GUNDI_OAUTH_ISSUER</code>.</p> <p>Audience parameter</p> <p><code>GUNDI_OAUTH_AUDIENCE</code> is required by some IdPs (notably Auth0 won't issue a usable API access token without it) and ignored by others (Keycloak password grant). Set it if your IdP requires it; leave it unset otherwise.</p>"},{"location":"getting-started/auth-setup/#loading-the-env-file","title":"Loading the <code>.env</code> file","text":"<p>The library calls <code>environs.Env.read_env()</code> the first time <code>gundi_client_v2.settings</code> is imported. By default this reads <code>./.env</code> from the current working directory. Run your script from the directory containing <code>.env</code>, or set <code>GUNDI_CLIENT_ENVFILE=/path/to/your.env</code> to point at a specific file.</p>"},{"location":"getting-started/auth-setup/#next","title":"Next","text":"<p>\u2192 First request \u2014 list the Connections in your account to verify everything works.</p>"},{"location":"getting-started/first-request/","title":"First request","text":"<p>This page walks through a minimal end-to-end example: authenticate and list the Connections in your Gundi account. If you haven't yet set environment variables, do Authentication setup first.</p>"},{"location":"getting-started/first-request/#smoke-test","title":"Smoke test","text":"<p>Save this as <code>smoke.py</code> in the same directory as your <code>.env</code>:</p> <pre><code>import asyncio\nimport json\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        connections = await client.get_connections()\n        print(f\"Found {len(connections)} connections\")\n        if connections:\n            # Print the first one as JSON\n            print(json.dumps(connections[0].dict(), default=str, indent=2))\n\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n</code></pre> <p>Run it:</p> <pre><code>python smoke.py\n</code></pre> <p>You should see a connection count and a JSON dump of the first Connection.</p>"},{"location":"getting-started/first-request/#what-just-happened","title":"What just happened","text":"<ol> <li><code>GundiClient()</code> read your env vars (<code>GUNDI_API_BASE_URL</code>,    <code>GUNDI_OAUTH_ISSUER</code>, and the credentials) into a configured client.</li> <li><code>async with</code> opened the underlying HTTPX session. No network    traffic yet.</li> <li><code>client.get_connections()</code> triggered the first authenticated    request. The client fetched the IdP's    <code>/.well-known/openid-configuration</code> document (OIDC discovery),    obtained an access token, then GET'd <code>/v2/connections/</code> on your    Gundi API and parsed the response into a list of <code>Connection</code>    Pydantic models.</li> <li>Exiting the context closed the HTTPX session cleanly.</li> </ol>"},{"location":"getting-started/first-request/#filtering-and-pagination","title":"Filtering and pagination","text":"<p><code>get_connections()</code> accepts a <code>params</code> dict that's passed straight to the API as query-string parameters. For example, filtering to only \"healthy\" connections (other status values include <code>unhealthy</code> and <code>disabled</code>):</p> <pre><code>healthy = await client.get_connections(params={\"status\": \"healthy\"})\n</code></pre> <p>For the full set of filters and pagination patterns, see Reading data \u2192 Connections.</p>"},{"location":"getting-started/first-request/#next","title":"Next","text":"<ul> <li>Learn how Gundi models its data \u2192 Gundi data model</li> <li>Dive into the auth grants \u2192 Authentication overview</li> <li>See more <code>Connection</code> examples \u2192 Reading Connections</li> </ul>"},{"location":"getting-started/install/","title":"Installation","text":""},{"location":"getting-started/install/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or newer. Check with <code>python3 --version</code>.</li> <li>A package installer \u2014 <code>pip</code> or <code>uv</code> (recommended for speed).</li> </ul>"},{"location":"getting-started/install/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>uv pip install gundi-client-v2\n</code></pre> <p>Or with plain pip:</p> <pre><code>pip install gundi-client-v2\n</code></pre> <p>Pin to a specific version with <code>gundi-client-v2==X.Y.Z</code>. The latest release is on the PyPI project page.</p>"},{"location":"getting-started/install/#install-from-source","title":"Install from source","text":"<p>For development, or to follow the <code>v2</code> branch directly:</p> <pre><code>pip install \"gundi-client-v2 @ git+https://github.com/PADAS/gundi-client.git@v2\"\n</code></pre> <p>To pin to a specific commit or tag, replace <code>@v2</code> with <code>@&lt;sha&gt;</code> or <code>@v3.0.0</code>.</p>"},{"location":"getting-started/install/#compatibility","title":"Compatibility","text":"Package Required Python <code>&gt;=3.10</code> <code>httpx</code> <code>&gt;=0.28</code> <code>pydantic</code> <code>&gt;=1.10,&lt;2</code> (pydantic v1 line) <code>gundi-core</code> <code>&gt;=1.5.8,&lt;3</code> <p>If your app uses FastAPI, you'll also need <code>fastapi&gt;=0.110.3</code> and <code>starlette&gt;=0.37</code>. Older versions ship a <code>TestClient</code> that calls <code>httpx.Client(app=...)</code> \u2014 a constructor argument that <code>httpx&gt;=0.28</code> removed. See the Migration guide for the full upgrade triangle.</p>"},{"location":"getting-started/install/#verify","title":"Verify","text":"<p>After installing, confirm the import succeeds:</p> <pre><code>python -c \"import gundi_client_v2; print(gundi_client_v2.__version__)\"\n</code></pre> <p>Expected output: the installed version number (e.g. <code>3.0.0</code>).</p>"},{"location":"getting-started/install/#next","title":"Next","text":"<p>\u2192 Authentication setup \u2014 configure credentials so the client can talk to Gundi.</p>"},{"location":"reading-data/connections/","title":"Reading Connections","text":"<p>A Connection groups one or more provider Integrations with their destination Integrations and the Routes between them. See Gundi data model for the conceptual overview.</p> <p>The client exposes two methods for reading Connections:</p> Method Returns Notes <code>get_connections(params=None)</code> <code>List[Connection]</code> Lists all Connections accessible to the authenticated principal <code>get_connection_details(integration_id)</code> <code>Connection</code> Fetches a single Connection by its provider Integration's ID"},{"location":"reading-data/connections/#list-all-connections","title":"List all connections","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        connections = await client.get_connections()\n        for c in connections:\n            print(c.id, c.provider.name, c.status)\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"reading-data/connections/#filtering","title":"Filtering","text":"<p><code>get_connections()</code> accepts a <code>params</code> dict that's passed through to the API as query-string parameters. Common filters:</p> <pre><code># Only healthy connections\nhealthy = await client.get_connections(params={\"status\": \"healthy\"})\n\n# Only connections owned by a specific organization\nowned = await client.get_connections(params={\"owner\": \"&lt;organization-id&gt;\"})\n\n# Search by name (substring match)\nnamed = await client.get_connections(params={\"search\": \"TrapTagger\"})\n</code></pre> <p>The exact set of available filters depends on the Gundi API version your deployment exposes. Consult your deployment's API docs for the complete list; pass anything documented there as a key in <code>params</code>.</p>"},{"location":"reading-data/connections/#pagination","title":"Pagination","text":"<p><code>get_connections()</code> does not paginate automatically \u2014 it returns the first page of results only. The Gundi API's default page size is currently 20. If your deployment has more Connections than fit in one page and you need them all, paginate manually using the API's cursor parameters:</p> <pre><code>all_connections = []\nparams = {}\nwhile True:\n    page = await client.get_connections(params=params)\n    all_connections.extend(page)\n    # The cursor parameter name depends on your Gundi API version\n    # (typically \"cursor\" or \"page\"). Check the next/previous links in\n    # the raw response if you need to script over multiple pages.\n    if len(page) &lt; 20:\n        break\n    # ... advance cursor\n    break\n</code></pre> <p>For streaming-style iteration over a large result set, prefer <code>get_integrations()</code>, which is an async generator and walks pagination cursors transparently.</p>"},{"location":"reading-data/connections/#fetch-one-connection","title":"Fetch one connection","text":"<pre><code>connection = await client.get_connection_details(\n    integration_id=\"ddd0946d-15b0-4308-b93d-e0470b6d33b6\"\n)\nprint(connection.dict())\n</code></pre> <p>The <code>integration_id</code> is the ID of the provider Integration. If you don't have it, list connections and pick the one you want by name or status:</p> <pre><code>connections = await client.get_connections(params={\"status\": \"healthy\"})\ntarget = next(c for c in connections if c.provider.name == \"TrapTagger PADAS\")\nprint(target.id)\n</code></pre>"},{"location":"reading-data/connections/#the-connection-model","title":"The <code>Connection</code> model","text":"<p>Defined in <code>gundi_core.schemas.v2.Connection</code>. Key fields:</p> Field Type Description <code>id</code> <code>UUID</code> Connection's unique identifier <code>status</code> <code>str</code> <code>healthy</code>, <code>unhealthy</code>, <code>disabled</code>, or <code>unknown</code> <code>provider</code> <code>ConnectionIntegration</code> Shallow view of the provider Integration (id, name, type, owner, base_url, status). For the full Integration record use <code>get_integration_details()</code>. <code>destinations</code> <code>List[ConnectionIntegration]</code> Shallow views of destination Integrations <code>routing_rules</code> <code>List[ConnectionRoute]</code> Lightweight references to Routes; each has <code>id</code> and <code>name</code> <code>default_route</code> <code>Optional[ConnectionRoute]</code> The Route used when no other rule matches <code>owner</code> <code>Organization</code> The owning Organization <p>For the canonical field list, see the <code>gundi-core</code> source.</p>"},{"location":"reading-data/connections/#output-as-json","title":"Output as JSON","text":"<p><code>Connection</code> is a Pydantic v1 model \u2014 call <code>.dict()</code> to get a plain dict, or serialize directly with <code>json.dumps</code>:</p> <pre><code>import json\n\nconnections = await client.get_connections()\nprint(json.dumps([c.dict() for c in connections], default=str, indent=2))\n</code></pre> <p><code>default=str</code> handles <code>UUID</code>, <code>datetime</code>, and other non-JSON-native types.</p>"},{"location":"reading-data/connections/#see-also","title":"See also","text":"<ul> <li>Reading Integrations for individual provider/destination details</li> <li>Connection example script in the repo</li> </ul>"},{"location":"reading-data/integrations/","title":"Reading Integrations","text":"<p>An Integration is a single registered third-party system \u2014 either a provider (e.g. a TrapTagger camera trap) or a destination (e.g. an EarthRanger site). See Gundi data model for context.</p> <p>The client exposes three methods:</p> Method Returns Notes <code>get_integrations(params=None)</code> <code>AsyncGenerator[Integration, None]</code> Streams all Integrations matching the filters <code>get_integration_details(integration_id)</code> <code>Integration</code> Fetches one Integration by ID <code>get_integration_api_key(integration_id)</code> <code>str</code> The Integration's API key. Used to bootstrap a <code>GundiDataSenderClient</code>."},{"location":"reading-data/integrations/#list-integrations-async-generator","title":"List integrations (async generator)","text":"<p>Unlike <code>get_connections</code>, <code>get_integrations</code> is an async generator \u2014 iterate it with <code>async for</code>:</p> <pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        async for integration in client.get_integrations():\n            print(integration.id, integration.name, integration.type)\n\n\nasyncio.run(main())\n</code></pre> <p>Why a generator? The Integrations endpoint paginates aggressively for large deployments. Streaming lets your code start processing as the first page arrives rather than waiting for the full list.</p> <p>If you need a list anyway, materialize it:</p> <pre><code>integrations = [i async for i in client.get_integrations()]\n</code></pre> <p>Don't <code>await</code> the generator itself</p> <p><code>get_integrations()</code> returns an async generator, not a coroutine. Use <code>async for</code> or an async list comprehension. <code>await client.get_integrations()</code> will fail at runtime.</p>"},{"location":"reading-data/integrations/#filtering","title":"Filtering","text":"<pre><code># Only provider-type Integrations\nasync for i in client.get_integrations(params={\"action_type\": \"pull_observations\"}):\n    print(i.name)\n\n# Search by name substring\nasync for i in client.get_integrations(params={\"search\": \"lotek\"}):\n    print(i.name, i.id)\n</code></pre> <p>Consult your Gundi API's documentation for the full set of filter keys.</p>"},{"location":"reading-data/integrations/#fetch-one-integration","title":"Fetch one integration","text":"<pre><code>integration = await client.get_integration_details(\n    integration_id=\"338225f3-91f9-4fe1-b013-353a229ce504\"\n)\nprint(integration.dict())\n</code></pre>"},{"location":"reading-data/integrations/#get-an-integrations-api-key","title":"Get an Integration's API key","text":"<p>Some workflows need the per-Integration API key to send observations or events directly through Gundi's data ingestion API. Fetch it with:</p> <pre><code>api_key = await client.get_integration_api_key(\n    integration_id=\"338225f3-91f9-4fe1-b013-353a229ce504\"\n)\n</code></pre> <p>Then pass it to <code>GundiDataSenderClient</code>:</p> <pre><code>from gundi_client_v2 import GundiDataSenderClient\n\nasync with GundiDataSenderClient(integration_api_key=api_key) as sender:\n    await sender.post_observations(data=[{...}])\n</code></pre> <p>(Sending observations is covered in Phase 2 of these docs.)</p>"},{"location":"reading-data/integrations/#the-integration-model","title":"The <code>Integration</code> model","text":"<p>Defined in <code>gundi_core.schemas.v2.Integration</code>. Key fields:</p> Field Type Description <code>id</code> <code>UUID</code> Integration's unique identifier <code>name</code> <code>str</code> Human-readable name <code>type</code> <code>IntegrationType</code> Slug + display name of the system type <code>base_url</code> <code>str</code> URL of the third-party system, if applicable <code>enabled</code> <code>bool</code> Whether Gundi is actively pulling/pushing <code>owner</code> <code>Organization</code> The owning Organization <code>configurations</code> <code>List[IntegrationActionConfiguration]</code> Per-action configuration <code>webhook_configuration</code> <code>Optional[IntegrationWebhookConfiguration]</code> Webhook-specific config <p>For the canonical field list, see the <code>gundi-core</code> source.</p>"},{"location":"reading-data/integrations/#output-as-json","title":"Output as JSON","text":"<pre><code>import json\n\nintegrations = [i async for i in client.get_integrations()]\nprint(json.dumps([i.dict() for i in integrations], default=str, indent=2))\n</code></pre>"},{"location":"reading-data/integrations/#see-also","title":"See also","text":"<ul> <li>Reading Connections \u2014 group Integrations by provider/destination</li> <li>Send observations example</li> </ul>"},{"location":"reading-data/routes/","title":"Reading Routes","text":"<p>A Route maps one or more provider Integrations to destination Integrations and optionally transforms payloads in transit (typically via a JQ filter). See Gundi data model for the conceptual overview.</p> <p>The client exposes a full CRUD surface for Routes:</p> Method Returns Notes <code>get_routes(params=None)</code> <code>List[Route]</code> First page only. <code>get_route_details(route_id)</code> <code>Route</code> Fetches by ID. <code>get_routes_for_connection(connection_id)</code> <code>List[Route]</code> Convenience wrapper for <code>get_routes(params={\"provider\": connection_id})</code>. First page only. <code>create_route(data)</code> <code>Route</code> POST. <code>update_route(route_id, data)</code> <code>Route</code> PATCH (partial updates supported). <code>delete_route(route_id)</code> <code>None</code> DELETE; returns None on success."},{"location":"reading-data/routes/#list-routes","title":"List routes","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        routes = await client.get_routes()\n        for r in routes:\n            print(r.id, r.name, len(r.data_providers), \"provider(s)\")\n\n\nasyncio.run(main())\n</code></pre> <p><code>get_routes()</code> returns the first page of results only. The Gundi API's default page size is 20. If you need to walk multiple pages, see the Pagination recipe for the cursor-walking approach.</p>"},{"location":"reading-data/routes/#filtering","title":"Filtering","text":"<p><code>get_routes()</code> accepts a <code>params</code> dict that is forwarded to the API as query-string parameters. Common filters:</p> <pre><code># Routes where a specific Integration is a provider\nprovider_routes = await client.get_routes(\n    params={\"provider\": \"&lt;provider-integration-id&gt;\"}\n)\n\n# Routes that deliver to a specific destination\ndest_routes = await client.get_routes(\n    params={\"destination\": \"&lt;destination-integration-id&gt;\"}\n)\n\n# Routes owned by a specific organization\norg_routes = await client.get_routes(\n    params={\"owner\": \"&lt;organization-id&gt;\"}\n)\n</code></pre> <p><code>get_routes_for_connection(connection_id)</code> is a convenience wrapper that calls <code>get_routes(params={\"provider\": str(connection_id)})</code> for you.</p>"},{"location":"reading-data/routes/#fetch-one-route","title":"Fetch one route","text":"<pre><code>route = await client.get_route_details(\n    route_id=\"a1b2c3d4-0000-0000-0000-000000000000\"\n)\nprint(route.name, route.configuration)\n</code></pre>"},{"location":"reading-data/routes/#create-a-route","title":"Create a route","text":"<p><code>create_route()</code> accepts a dict whose shape follows the Gundi API's route creation endpoint. The <code>data_providers</code> and <code>destinations</code> fields take raw Integration IDs (strings or UUIDs) in the input dict:</p> <pre><code>route = await client.create_route(data={\n    \"name\": \"TrapTagger \u2192 ER (events)\",\n    \"owner\": \"&lt;organization-id&gt;\",\n    \"data_providers\": [\"&lt;provider-integration-id&gt;\"],\n    \"destinations\": [\"&lt;destination-integration-id&gt;\"],\n})\nprint(route.id)\n</code></pre> <p>The returned <code>Route</code> object has <code>data_providers</code> and <code>destinations</code> populated as <code>ConnectionIntegration</code> objects (not raw IDs). The same applies to <code>update_route()</code>.</p>"},{"location":"reading-data/routes/#update-a-route","title":"Update a route","text":"<p><code>update_route()</code> uses HTTP PATCH, so you can send a partial dict \u2014 only the fields you include are changed:</p> <pre><code>route = await client.update_route(\n    route_id=\"&lt;route-id&gt;\",\n    data={\"name\": \"Renamed\"},\n)\nprint(route.name)\n</code></pre> <p>You can update any of the writable fields via the same partial-dict pattern. For transformation logic (the <code>configuration</code> field), the inner <code>data</code> shape is deployment-specific \u2014 consult your deployment's documentation for the exact structure.</p>"},{"location":"reading-data/routes/#delete-a-route","title":"Delete a route","text":"<pre><code>await client.delete_route(route_id=route.id)\n# Returns None on success (HTTP 204 No Content)\n</code></pre> <p><code>delete_route()</code> returns <code>None</code> on success. There is no response body to inspect. If the route does not exist or you do not have permission to delete it, a <code>GundiAPIError</code> is raised.</p>"},{"location":"reading-data/routes/#the-route-model","title":"The <code>Route</code> model","text":"<p>Defined in <code>gundi_core.schemas.v2.Route</code>. Key fields:</p> Field Type Description <code>id</code> <code>UUID</code> Route ID <code>name</code> <code>str</code> Human-readable name <code>owner</code> <code>Optional[UUID]</code> Organization ID <code>data_providers</code> <code>List[ConnectionIntegration]</code> Provider Integrations <code>destinations</code> <code>List[ConnectionIntegration]</code> Destination Integrations <code>configuration</code> <code>RouteConfiguration</code> Transformation rules (often a JQ filter) <code>additional</code> <code>Dict[str, Any]</code> Free-form extras <p><code>ConnectionIntegration</code> is the same shallow view used in the <code>Connection</code> model: it carries <code>id</code>, <code>name</code>, <code>type</code>, <code>owner</code>, <code>base_url</code>, and <code>status</code>. For the full Integration record, call <code>get_integration_details()</code>.</p> <p>For the canonical field list, see the <code>gundi-core</code> source.</p>"},{"location":"reading-data/routes/#output-as-json","title":"Output as JSON","text":"<pre><code>import json\n\nroutes = await client.get_routes()\nprint(json.dumps([r.dict() for r in routes], default=str, indent=2))\n</code></pre> <p><code>default=str</code> handles <code>UUID</code> and other non-JSON-native types that Pydantic v1 stores as objects.</p>"},{"location":"reading-data/routes/#see-also","title":"See also","text":"<ul> <li>Gundi data model \u2014 Integration, Connection, and   Route vocabulary</li> <li>Reading Connections \u2014 Connections group providers and   destinations; Routes define the wiring between them</li> <li>Reading Integrations \u2014 fetch full Integration details from   the IDs in <code>data_providers</code> / <code>destinations</code></li> </ul>"},{"location":"reading-data/traces/","title":"Reading Traces","text":"<p>A Trace is the record Gundi writes each time a payload crosses a provider \u2192 destination edge. Traces let you confirm delivery, diagnose errors, and look up the ID the destination assigned to a forwarded observation or event.</p> <p>For the conceptual background \u2014 how payloads move through Gundi and why Traces exist \u2014 see Tracing and lifecycle. This page covers the API.</p> Method Returns Notes <code>get_traces(params)</code> <code>List[GundiTrace]</code> First page only."},{"location":"reading-data/traces/#listing-traces","title":"Listing traces","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        traces = await client.get_traces(params={\n            \"object_id\": \"&lt;gundi-observation-id&gt;\",\n        })\n        for t in traces:\n            print(t.object_id, \"\u2192\", t.destination, t.delivered_at)\n\n\nasyncio.run(main())\n</code></pre> <p><code>get_traces()</code> returns the first page of results only. The default page size is 20. Pass cursor parameters in <code>params</code> to walk additional pages if your query matches more results than fit in one page.</p>"},{"location":"reading-data/traces/#common-filters","title":"Common filters","text":"<p><code>get_traces()</code> accepts a <code>params</code> dict that is passed through to the API as query-string parameters. Useful filters:</p> <pre><code># All traces for a specific payload\ntraces = await client.get_traces(params={\"object_id\": \"&lt;gundi-object-id&gt;\"})\n\n# Traces going to a specific destination Integration\n# Note: the parameter name is deployment-dependent \u2014 try \"destination\" first;\n# some API versions expose it as \"destination_id\".\ntraces = await client.get_traces(params={\"destination\": \"&lt;destination-integration-id&gt;\"})\n\n# Traces from a specific provider Integration\ntraces = await client.get_traces(params={\"data_provider\": \"&lt;provider-integration-id&gt;\"})\n\n# Only traces that errored\ntraces = await client.get_traces(params={\"has_error\": True})\n\n# Combine filters \u2014 the API treats multiple keys as AND\ntraces = await client.get_traces(params={\n    \"destination\": \"&lt;destination-integration-id&gt;\",\n    \"has_error\": True,\n})\n</code></pre> <p>Time-range filters (e.g. <code>delivered_at__gte</code>) may be available depending on your Gundi deployment version. Consult your deployment's API documentation for the complete filter surface.</p>"},{"location":"reading-data/traces/#the-gunditrace-model","title":"The <code>GundiTrace</code> model","text":"<p>Defined in <code>gundi_core.schemas.v2.GundiTrace</code>. Fields:</p> Field Type Description <code>object_id</code> <code>Union[UUID, str]</code> Gundi-side ID of the source observation/event <code>object_type</code> <code>str</code> Payload type discriminator <code>related_to</code> <code>Optional[Union[UUID, str]]</code> Parent object ID <code>data_provider</code> <code>Optional[Union[UUID, str]]</code> Provider Integration ID <code>destination</code> <code>Optional[Union[UUID, str]]</code> Destination Integration ID <code>delivered_at</code> <code>datetime</code> When the payload reached the destination <code>external_id</code> <code>Optional[Union[UUID, str]]</code> The ID assigned by the destination <code>created_at</code> <code>datetime</code> When Gundi recorded the trace <code>updated_at</code> <code>datetime</code> Last update to the trace <code>last_update_delivered_at</code> <code>datetime</code> Time of the most recent successful update <code>is_duplicate</code> <code>bool</code> True if Gundi suppressed as duplicate <code>has_error</code> <code>bool</code> True if delivery errored <p>For the canonical field list, see the <code>gundi-core</code> source.</p>"},{"location":"reading-data/traces/#use-cases","title":"Use cases","text":""},{"location":"reading-data/traces/#confirm-a-specific-observation-was-delivered","title":"Confirm a specific observation was delivered","text":"<p>Filter by <code>object_id</code> and inspect each trace's status flags:</p> <pre><code>traces = await client.get_traces(params={\"object_id\": \"&lt;gundi-object-id&gt;\"})\n\nfor t in traces:\n    if t.is_duplicate:\n        print(f\"Suppressed as duplicate at {t.destination}\")\n    elif t.has_error:\n        print(f\"Delivery to {t.destination} errored\")\n    else:\n        print(f\"Delivered to {t.destination} at {t.delivered_at}\")\n</code></pre>"},{"location":"reading-data/traces/#find-all-errors-for-a-destination","title":"Find all errors for a destination","text":"<p>Combine <code>destination</code> and <code>has_error</code> to pull the error queue for a single destination Integration:</p> <pre><code>error_traces = await client.get_traces(params={\n    \"destination\": \"&lt;destination-integration-id&gt;\",\n    \"has_error\": True,\n})\nfor t in error_traces:\n    print(t.object_id, t.object_type)\n</code></pre>"},{"location":"reading-data/traces/#look-up-the-destinations-external-id-for-a-gundi-observation","title":"Look up the destination's external ID for a Gundi observation","text":"<p>After Gundi delivers a payload, the destination assigns its own ID (for example an EarthRanger event UUID). Retrieve it via <code>external_id</code>:</p> <pre><code>traces = await client.get_traces(params={\"object_id\": \"&lt;gundi-object-id&gt;\"})\n\nfor t in traces:\n    if t.external_id and not t.has_error:\n        print(f\"Destination {t.destination} assigned ID: {t.external_id}\")\n</code></pre> <p>Use <code>external_id</code> to correlate Gundi-side <code>object_id</code> values with destination-side records without querying the destination directly.</p>"},{"location":"reading-data/traces/#output-as-json","title":"Output as JSON","text":"<pre><code>import json\n\ntraces = await client.get_traces(params={\"object_id\": \"&lt;gundi-object-id&gt;\"})\nprint(json.dumps([t.dict() for t in traces], default=str, indent=2))\n</code></pre>"},{"location":"reading-data/traces/#see-also","title":"See also","text":"<ul> <li>Tracing and lifecycle \u2014 how payloads   move through Gundi and why Traces are recorded</li> </ul>"},{"location":"recipes/custom-httpx/","title":"Custom HTTPX client","text":"<p><code>GundiClient</code> builds and owns its HTTPX session internally. You cannot pass a pre-built <code>httpx.AsyncClient</code> instance, but several constructor kwargs let you tune the session's behavior.</p>"},{"location":"recipes/custom-httpx/#timeouts","title":"Timeouts","text":"<p>Two separate timeout knobs control how long the client waits:</p> <ul> <li><code>connect_timeout</code> \u2014 TCP connect timeout (default: 3.1 seconds)</li> <li><code>data_timeout</code> \u2014 read/write timeout per request (default: 20 seconds)</li> </ul> <pre><code>from gundi_client_v2 import GundiClient\n\nclient = GundiClient(\n    connect_timeout=10.0,\n    data_timeout=60.0,\n)\n</code></pre> <p>Increase <code>data_timeout</code> if you're hitting timeouts on large read operations against a slow deployment. Increase <code>connect_timeout</code> if you're on a high-latency network.</p>"},{"location":"recipes/custom-httpx/#retries","title":"Retries","text":"<p><code>max_http_retries</code> sets the number of transport-layer connection retries (default: 5):</p> <pre><code>client = GundiClient(\n    max_http_retries=3,\n)\n</code></pre> <p>These are TCP-level retries handled by <code>httpx.AsyncHTTPTransport</code>, not application-level retries for 4xx/5xx HTTP responses. For retrying on HTTP errors, use a library like <code>stamina</code> \u2014 see Error handling.</p>"},{"location":"recipes/custom-httpx/#ssl-verification","title":"SSL verification","text":"<p><code>use_ssl</code> controls certificate verification (default: True):</p> <pre><code>client = GundiClient(\n    use_ssl=False,\n)\n</code></pre> <p>Only disable this for local development against a self-signed IdP or a test environment without valid certificates. Never set <code>use_ssl=False</code> in production.</p>"},{"location":"recipes/custom-httpx/#combining-options","title":"Combining options","text":"<p>All kwargs can be passed in the same constructor call:</p> <pre><code>async with GundiClient(\n    connect_timeout=10.0,\n    data_timeout=60.0,\n    max_http_retries=3,\n) as client:\n    connections = await client.get_connections()\n</code></pre>"},{"location":"recipes/custom-httpx/#what-you-cannot-customize","title":"What you cannot customize","text":"<p>There is no kwarg to:</p> <ul> <li>Pass a pre-built <code>httpx.AsyncClient</code> instance</li> <li>Supply a custom HTTPX transport (e.g. a proxy transport)</li> <li>Set a proxy URL</li> </ul> <p>If you need any of these, open an issue at github.com/PADAS/gundi-client/issues.</p>"},{"location":"recipes/custom-httpx/#related-pages","title":"Related pages","text":"<ul> <li>Troubleshooting</li> <li>Error handling</li> </ul>"},{"location":"recipes/error-handling/","title":"Error handling","text":"<p><code>gundi_client_v2</code> raises a small, consistent exception hierarchy. Every error the library surfaces is a subclass of <code>GundiClientError</code>.</p>"},{"location":"recipes/error-handling/#the-exception-hierarchy","title":"The exception hierarchy","text":"<pre><code>GundiClientError\n  \u251c\u2500\u2500 AuthenticationError       # OAuth/token failures\n  \u2514\u2500\u2500 GundiAPIError             # 4xx/5xx HTTP responses\n</code></pre> <ul> <li><code>AuthenticationError</code> \u2014 raised when the OAuth token request fails (bad   credentials, unreachable IdP, misconfigured issuer URL, etc.).</li> <li><code>GundiAPIError</code> \u2014 raised when the Gundi API returns a 4xx or 5xx   response. Exposes <code>.status_code: int</code> and <code>.detail: str</code>.</li> </ul>"},{"location":"recipes/error-handling/#catching-by-category","title":"Catching by category","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\nfrom gundi_client_v2.errors import (\n    AuthenticationError,\n    GundiAPIError,\n    GundiClientError,\n)\n\n\nasync def main():\n    try:\n        async with GundiClient() as client:\n            connections = await client.get_connections()\n    except AuthenticationError as e:\n        # Token request failed; check credentials or client configuration\n        print(f\"Auth error: {e}\")\n    except GundiAPIError as e:\n        # 4xx/5xx from the Gundi API\n        print(f\"Status {e.status_code}, detail: {e.detail}\")\n    except GundiClientError as e:\n        # Anything else from the library (rare)\n        print(f\"Client error: {e}\")\n\n\nasyncio.run(main())\n</code></pre> <p>Catch the most specific exception first (<code>AuthenticationError</code>, <code>GundiAPIError</code>), then <code>GundiClientError</code> as a catch-all for anything unexpected from the library.</p>"},{"location":"recipes/error-handling/#handling-specific-status-codes","title":"Handling specific status codes","text":"<p><code>GundiAPIError.status_code</code> lets you branch on the HTTP status:</p> <pre><code>except GundiAPIError as e:\n    if e.status_code == 404:\n        # Resource doesn't exist (or you don't have access)\n        ...\n    elif e.status_code == 403:\n        # Permission denied\n        ...\n    elif e.status_code &gt;= 500:\n        # Server-side issue; consider retrying\n        ...\n</code></pre> <p><code>GundiAPIError.detail</code> contains the body text returned by the API, which typically has a human-readable description of the problem.</p>"},{"location":"recipes/error-handling/#logging-useful-context","title":"Logging useful context","text":"<p>Log <code>status_code</code> and <code>detail</code> alongside the operation that failed:</p> <pre><code>import logging\n\nlogger = logging.getLogger(__name__)\n\n\nasync def fetch_integration(client, integration_id):\n    try:\n        return await client.get_integration_details(integration_id)\n    except GundiAPIError as e:\n        logger.error(\n            \"Failed to fetch integration\",\n            extra={\n                \"integration_id\": integration_id,\n                \"status_code\": e.status_code,\n                \"detail\": e.detail,\n            },\n        )\n        raise\n</code></pre> <p>Re-raising preserves the original traceback for upstream handlers or monitoring tools.</p>"},{"location":"recipes/error-handling/#retry-strategies","title":"Retry strategies","text":"<p>For transient errors (5xx responses, network blips), retry only on those \u2014 not on 4xx errors that won't resolve on a second attempt. The <code>stamina</code> library provides exponential back-off with jitter and accepts a predicate for the <code>on=</code> parameter to filter which exceptions trigger a retry:</p> <p>First install stamina (<code>pip install stamina</code>):</p> <pre><code>import stamina\nfrom gundi_client_v2 import GundiClient\nfrom gundi_client_v2.errors import GundiAPIError\n\n\ndef _is_transient(exc: BaseException) -&gt; bool:\n    \"\"\"Retry only on 5xx server errors.\"\"\"\n    return isinstance(exc, GundiAPIError) and exc.status_code &gt;= 500\n\n\n@stamina.retry(on=_is_transient, attempts=3)\nasync def list_connections(client: GundiClient) -&gt; list:\n    return await client.get_connections()\n\n\nasync def main():\n    async with GundiClient() as client:\n        connections = await list_connections(client)\n</code></pre> <p>Be selective about what you retry:</p> <ul> <li>Do retry: 5xx server errors \u2014 they indicate a transient server-side   problem that may resolve on the next attempt.</li> <li>Do not retry: 4xx errors \u2014 they indicate a client-side problem   (bad request, wrong ID, missing permission) that won't resolve by   retrying. The predicate above ignores them.</li> </ul>"},{"location":"recipes/error-handling/#related-pages","title":"Related pages","text":"<ul> <li>Authentication: Refresh and errors</li> <li>Troubleshooting</li> </ul>"},{"location":"recipes/filtering/","title":"Filtering","text":"<p>All <code>get_*</code> methods on <code>GundiClient</code> accept an optional <code>params</code> dict that is forwarded as query parameters to the Gundi API. The API treats multiple keys in the same dict as AND conditions.</p>"},{"location":"recipes/filtering/#status-filters","title":"Status filters","text":"<p>Filter Connections by health status:</p> <pre><code>async with GundiClient() as client:\n    healthy = await client.get_connections(params={\"status\": \"healthy\"})\n    broken = await client.get_connections(params={\"status\": \"unhealthy\"})\n</code></pre> <p>Valid values for <code>status</code>: <code>healthy</code>, <code>unhealthy</code>, <code>disabled</code>.</p>"},{"location":"recipes/filtering/#owner-filters","title":"Owner filters","text":"<p>Scope results to a specific organization:</p> <pre><code>async with GundiClient() as client:\n    org_connections = await client.get_connections(\n        params={\"owner\": \"&lt;organization-id&gt;\"}\n    )\n    org_integrations = [\n        i async for i in client.get_integrations(\n            params={\"owner\": \"&lt;organization-id&gt;\"}\n        )\n    ]\n</code></pre> <p>The <code>owner</code> filter works across Connections, Integrations, and Routes.</p>"},{"location":"recipes/filtering/#substring-search","title":"Substring search","text":"<p>Most list endpoints support a <code>search</code> parameter for substring matching on the resource name:</p> <pre><code>async with GundiClient() as client:\n    async for integration in client.get_integrations(params={\"search\": \"lotek\"}):\n        print(integration.name)\n</code></pre>"},{"location":"recipes/filtering/#provider-and-destination-filters-on-routes","title":"Provider and destination filters on Routes","text":"<p>Narrow Routes to those involving a specific provider or destination Integration:</p> <pre><code>async with GundiClient() as client:\n    # Routes where this integration is a provider\n    provider_routes = await client.get_routes(\n        params={\"provider\": \"&lt;integration-id&gt;\"}\n    )\n\n    # Routes where this integration is a destination\n    dest_routes = await client.get_routes(\n        params={\"destination\": \"&lt;integration-id&gt;\"}\n    )\n</code></pre> <p><code>get_routes_for_connection(connection_id)</code> is a convenience wrapper for <code>get_routes(params={\"provider\": str(connection_id)})</code>.</p>"},{"location":"recipes/filtering/#trace-filters","title":"Trace filters","text":"<p>Common trace queries:</p> <pre><code>async with GundiClient() as client:\n    # All traces for a specific Gundi observation\n    traces = await client.get_traces(\n        params={\"object_id\": \"&lt;gundi-observation-id&gt;\"}\n    )\n\n    # All error traces for a provider\n    error_traces = await client.get_traces(\n        params={\"data_provider\": \"&lt;provider-id&gt;\", \"has_error\": True}\n    )\n</code></pre>"},{"location":"recipes/filtering/#combining-filters","title":"Combining filters","text":"<p>Pass multiple keys in the same dict to AND them together:</p> <pre><code>async with GundiClient() as client:\n    connections = await client.get_connections(\n        params={\"status\": \"healthy\", \"owner\": \"&lt;organization-id&gt;\"}\n    )\n</code></pre>"},{"location":"recipes/filtering/#client-side-filtering","title":"Client-side filtering","text":"<p>When the server doesn't expose a filter you need, filter the returned list locally:</p> <pre><code>async with GundiClient() as client:\n    connections = await client.get_connections()\n    traptagger = [\n        c for c in connections\n        if c.provider.name.startswith(\"TrapTagger\")\n    ]\n</code></pre> <p>Client-side filtering only sees the first page of results. If your dataset spans multiple pages, either increase the page size (see Pagination) or use <code>get_integrations()</code> which walks all pages automatically.</p>"},{"location":"recipes/filtering/#related-pages","title":"Related pages","text":"<ul> <li>Reading Connections</li> <li>Reading Integrations</li> <li>Reading Routes</li> <li>Reading Traces</li> <li>Pagination</li> </ul>"},{"location":"recipes/pagination/","title":"Pagination","text":"<p>The Gundi client handles pagination differently depending on the method you're calling. <code>get_integrations()</code> walks all pages automatically; <code>get_connections()</code> and <code>get_routes()</code> return only the first page.</p>"},{"location":"recipes/pagination/#get_integrations-automatic-cursor-walking","title":"<code>get_integrations()</code> \u2014 automatic cursor walking","text":"<p><code>get_integrations()</code> is an async generator that transparently follows the Gundi API's <code>next</code> cursor. You iterate with <code>async for</code> and the client fetches subsequent pages on demand.</p> <pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient\n\n\nasync def main():\n    async with GundiClient() as client:\n        async for integration in client.get_integrations(params={\"search\": \"lotek\"}):\n            print(integration.id, integration.name)\n\n\nasyncio.run(main())\n</code></pre> <p>You can also materialize all results at once:</p> <pre><code>async with GundiClient() as client:\n    integrations = [i async for i in client.get_integrations()]\n</code></pre> <p>Note that materializing large result sets into a list consumes memory proportional to the total count. For large deployments, prefer streaming with <code>async for</code>.</p>"},{"location":"recipes/pagination/#get_connections-and-get_routes-first-page-only","title":"<code>get_connections()</code> and <code>get_routes()</code> \u2014 first page only","text":"<p><code>get_connections()</code> and <code>get_routes()</code> parse and return only the items from the first page of results.</p> <p>For these two methods, you'll need to handle pagination yourself. The Gundi API uses cursor-based pagination \u2014 the response includes a <code>next</code> URL when more pages exist \u2014 but <code>get_connections()</code> and <code>get_routes()</code> parse and return only the items from the first page. If you need to walk multiple pages, two options:</p> <ol> <li> <p>Increase the page size via a query parameter (the exact parameter    name varies by deployment \u2014 typically <code>limit</code> or <code>page_size</code>). This    is the simplest fix when your dataset is small to medium.</p> </li> <li> <p>Drop down to the raw HTTPX session on the client (<code>client._session</code>)    to call the endpoint directly, then walk the <code>next</code> URL yourself.    This bypasses the convenience method but lets you stream results.</p> </li> </ol> <p><code>_session</code> is private API</p> <p>The <code>_session</code> attribute is an internal implementation detail and may change between releases without notice. Treat this as a last-resort workaround until the library exposes a public pagination helper.</p> <p>See also Filtering for ways to narrow the result set before it reaches the pagination layer.</p>"},{"location":"recipes/pagination/#trade-offs","title":"Trade-offs","text":"Approach Pro Con <code>async for</code> generator Low memory; streams results Cannot <code>len()</code> without materializing List comprehension (<code>[i async for i in ...]</code>) Random access, slicing Holds all results in RAM Manual <code>_session</code> walk Full control over cursor Bypasses type parsing; more code"},{"location":"recipes/pagination/#related-pages","title":"Related pages","text":"<ul> <li>Reading Connections</li> <li>Reading Integrations</li> <li>Reading Routes</li> </ul>"},{"location":"sending-data/events/","title":"Sending Events","text":"<p>Events represent discrete incidents or reports \u2014 wildlife sightings, security alerts, ranger activities, etc. Use <code>GundiDataSenderClient.post_events()</code> to push them into Gundi, and <code>update_event()</code> to patch an existing event.</p>"},{"location":"sending-data/events/#prerequisites","title":"Prerequisites","text":"<p>You need a provider Integration API key to authenticate with the Sensors API. Retrieve it via <code>GundiClient.get_integration_api_key()</code> using the UUID of the relevant provider integration.</p> <p>See Reading \u2014 Integrations for how to look up integration IDs and call <code>get_integration_api_key</code>.</p> <p>API key may be <code>None</code></p> <p><code>get_integration_api_key</code> returns <code>None</code> when no API key has been configured for the integration. Always check before proceeding:</p> <pre><code>api_key = await portal.get_integration_api_key(integration_id=\"...\")\nif api_key is None:\n    raise RuntimeError(\"No API key configured for this integration\")\n</code></pre>"},{"location":"sending-data/events/#minimal-example","title":"Minimal example","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient, GundiDataSenderClient\n\n\nasync def main():\n    async with GundiClient() as portal:\n        api_key = await portal.get_integration_api_key(\n            integration_id=\"&lt;your-provider-integration-id&gt;\"\n        )\n        if api_key is None:\n            raise RuntimeError(\"No API key configured for this integration\")\n\n    sender = GundiDataSenderClient(integration_api_key=api_key)\n    response = await sender.post_events(data=[\n        {\n            \"event_type\": \"wildlife_sighting\",\n            \"recorded_at\": \"2026-06-01T12:34:56Z\",\n            \"location\": {\"lat\": -1.234, \"lon\": 36.789},\n            \"title\": \"Elephant near camp\",\n            \"event_details\": {\n                \"species\": \"African elephant\",\n                \"count\": 3,\n            },\n        },\n    ])\n    print(response)\n\n\nasyncio.run(main())\n</code></pre>"},{"location":"sending-data/events/#common-event-fields","title":"Common event fields","text":"Field Required Notes <code>event_type</code> Yes Event-type slug defined by your Gundi deployment <code>recorded_at</code> Yes ISO-8601 timestamp <code>location</code> Yes <code>{\"lat\": float, \"lon\": float}</code> <code>title</code> Recommended Human-readable summary <code>event_details</code> Optional Free-form dict matching the event-type schema <code>priority</code> Optional Numeric priority (0 = lowest) <code>state</code> Optional <code>new</code>, <code>active</code>, <code>resolved</code>, etc. <p>Event types are deployment-specific</p> <p>The set of valid <code>event_type</code> slugs and the expected <code>event_details</code> schema are defined by your Gundi deployment and destination. Check the destination's documentation for the full list.</p>"},{"location":"sending-data/events/#updating-an-existing-event","title":"Updating an existing event","text":"<p>Use <code>update_event(event_id, data)</code> (PATCH) to modify a previously posted event. Pass the Gundi-side <code>object_id</code> returned by a successful <code>post_events</code> call (or fetched via <code>get_traces</code>):</p> <pre><code>updated = await sender.update_event(\n    event_id=\"&lt;gundi-event-object-id&gt;\",\n    data={\"state\": \"resolved\"},\n)\n</code></pre> <p>object_id vs external_id</p> <p><code>event_id</code> is Gundi's internal <code>object_id</code>. The destination system's own ID is stored separately in the Trace record as <code>external_id</code> and is not used here.</p>"},{"location":"sending-data/events/#sending-event-attachments","title":"Sending event attachments","text":"<p>To attach files (photos, documents) to an existing event, see Sending Messages and Attachments.</p>"},{"location":"sending-data/events/#error-handling","title":"Error handling","text":"<p>Wrap calls in a <code>try/except</code> block to catch API errors:</p> <pre><code>from gundi_client_v2.errors import GundiAPIError\n\ntry:\n    response = await sender.post_events(data=[...])\nexcept GundiAPIError as exc:\n    print(f\"Gundi API error {exc.status_code}: {exc}\")\n</code></pre> <p><code>GundiDataSenderClient</code> uses a fixed 120-second HTTPX timeout.</p> <p>See Error handling for the full pattern and a list of exception types.</p>"},{"location":"sending-data/events/#related","title":"Related","text":"<ul> <li>Payload types \u2014 overview of observations, events, and messages</li> <li>Sending Observations</li> <li>Sending Messages and Attachments</li> </ul>"},{"location":"sending-data/messages-attachments/","title":"Sending Messages and Attachments","text":"<p>Gundi supports two distinct operations for sending supplemental data alongside observations and events:</p> <ul> <li>Messages \u2014 text messages sent via <code>post_messages()</code>, with optional location and timestamp</li> <li>Attachments \u2014 binary files (images, documents) attached to an existing event via <code>post_event_attachments()</code></li> </ul> <p>These use different methods and different payload shapes.</p>"},{"location":"sending-data/messages-attachments/#prerequisites","title":"Prerequisites","text":"<p>You need a provider Integration API key to authenticate with the Sensors API. Retrieve it via <code>GundiClient.get_integration_api_key()</code>.</p> <p>See Reading \u2014 Integrations for how to look up integration IDs.</p> <p>API key may be <code>None</code></p> <p><code>get_integration_api_key</code> returns <code>None</code> when no API key has been configured for the integration. Always check before proceeding:</p> <pre><code>api_key = await portal.get_integration_api_key(integration_id=\"...\")\nif api_key is None:\n    raise RuntimeError(\"No API key configured for this integration\")\n</code></pre>"},{"location":"sending-data/messages-attachments/#sending-a-message","title":"Sending a message","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient, GundiDataSenderClient\n\n\nasync def main():\n    async with GundiClient() as portal:\n        api_key = await portal.get_integration_api_key(\n            integration_id=\"&lt;your-provider-integration-id&gt;\"\n        )\n        if api_key is None:\n            raise RuntimeError(\"No API key configured for this integration\")\n\n    sender = GundiDataSenderClient(integration_api_key=api_key)\n    response = await sender.post_messages(data=[\n        {\n            \"sender\": \"ranger-1\",\n            \"recipients\": [\"ops-channel\"],\n            \"text\": \"Camera trap triggered at site B.\",\n            # Optional:\n            # \"recorded_at\": \"2026-06-01T12:34:56Z\",\n            # \"location\": {\"latitude\": -1.234, \"longitude\": 36.789},\n            # \"additional\": {\"status\": {\"lowBattery\": 1}},\n        },\n    ])\n    print(response)\n\n\nasyncio.run(main())\n</code></pre> <p>Coordinate key naming</p> <p>Messages use <code>latitude</code>/<code>longitude</code> (long form), while Observations and Events use <code>lat</code>/<code>lon</code> (short form). This inconsistency is in the Gundi sensors API itself \u2014 pass the form shown in this page's examples to avoid malformed payloads.</p>"},{"location":"sending-data/messages-attachments/#message-fields","title":"Message fields","text":"Field Required Notes <code>sender</code> Yes Identifier for the message sender <code>recipients</code> Yes List of recipient identifiers <code>text</code> Yes The message body <code>recorded_at</code> Optional ISO-8601 timestamp; defaults to ingestion time if omitted <code>location</code> Optional <code>{\"latitude\": float, \"longitude\": float}</code> <code>additional</code> Optional Free-form dict of additional attributes <p>Note that <code>recipients</code> is a list \u2014 even when targeting a single channel or address, wrap it in a list.</p>"},{"location":"sending-data/messages-attachments/#sending-an-attachment","title":"Sending an attachment","text":"<p>Attachments are binary files associated with a specific event. Use <code>post_event_attachments(event_id, attachments)</code> where each attachment is a <code>(filename, file_binary)</code> tuple:</p> <pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient, GundiDataSenderClient\n\n\nasync def main():\n    async with GundiClient() as portal:\n        api_key = await portal.get_integration_api_key(\n            integration_id=\"&lt;your-provider-integration-id&gt;\"\n        )\n        if api_key is None:\n            raise RuntimeError(\"No API key configured for this integration\")\n\n    with open(\"photo.jpg\", \"rb\") as f:\n        binary = f.read()\n\n    sender = GundiDataSenderClient(integration_api_key=api_key)\n    response = await sender.post_event_attachments(\n        event_id=\"&lt;gundi-event-object-id&gt;\",\n        attachments=[(\"photo.jpg\", binary)],\n    )\n    print(response)\n\n\nasyncio.run(main())\n</code></pre> <p>To send multiple attachments in a single call, add more tuples to the list:</p> <pre><code>response = await sender.post_event_attachments(\n    event_id=\"&lt;gundi-event-object-id&gt;\",\n    attachments=[\n        (\"photo1.jpg\", binary1),\n        (\"photo2.jpg\", binary2),\n    ],\n)\n</code></pre> <p>What gets sent on the wire</p> <p>Attachments are uploaded as a multipart form request. Each tuple becomes a <code>file</code> form field using the provided filename as the <code>Content-Disposition</code> filename.</p>"},{"location":"sending-data/messages-attachments/#limits","title":"Limits","text":"<p><code>GundiDataSenderClient</code> uses a fixed 120-second HTTPX timeout. For large files:</p> <ul> <li>Split the upload across multiple calls (one attachment at a time)</li> <li>Upload the file to external storage and reference the URL from the event's <code>event_details</code> field</li> </ul>"},{"location":"sending-data/messages-attachments/#error-handling","title":"Error handling","text":"<pre><code>from gundi_client_v2.errors import GundiAPIError\n\ntry:\n    response = await sender.post_event_attachments(event_id=\"...\", attachments=[...])\nexcept GundiAPIError as exc:\n    print(f\"Gundi API error {exc.status_code}: {exc}\")\n</code></pre> <p>See Error handling for the full pattern and a list of exception types.</p>"},{"location":"sending-data/messages-attachments/#related","title":"Related","text":"<ul> <li>Payload types \u2014 overview of observations, events, and messages</li> <li>Sending Observations</li> <li>Sending Events</li> </ul>"},{"location":"sending-data/observations/","title":"Sending Observations","text":"<p>Observations are timestamped, geo-located data points \u2014 typically position fixes from tracking devices. Use <code>GundiDataSenderClient.post_observations()</code> to push them into Gundi.</p>"},{"location":"sending-data/observations/#prerequisites","title":"Prerequisites","text":"<p>You need a provider Integration API key to authenticate with the Sensors API. Retrieve it via <code>GundiClient.get_integration_api_key()</code> using the UUID of the relevant provider integration.</p> <p>See Reading \u2014 Integrations for how to look up integration IDs and call <code>get_integration_api_key</code>.</p> <p>API key may be <code>None</code></p> <p><code>get_integration_api_key</code> returns <code>None</code> when no API key has been configured for the integration. Always check before proceeding:</p> <pre><code>api_key = await portal.get_integration_api_key(integration_id=\"...\")\nif api_key is None:\n    raise RuntimeError(\"No API key configured for this integration\")\n</code></pre>"},{"location":"sending-data/observations/#minimal-example","title":"Minimal example","text":"<pre><code>import asyncio\nfrom gundi_client_v2 import GundiClient, GundiDataSenderClient\n\n\nasync def main():\n    async with GundiClient() as portal:\n        api_key = await portal.get_integration_api_key(\n            integration_id=\"&lt;your-provider-integration-id&gt;\"\n        )\n        if api_key is None:\n            raise RuntimeError(\"No API key configured for this integration\")\n\n    sender = GundiDataSenderClient(integration_api_key=api_key)\n    response = await sender.post_observations(data=[\n        {\n            \"source\": \"device-001\",\n            \"type\": \"tracking-device\",\n            \"subject_type\": \"wildlife.elephant\",\n            \"recorded_at\": \"2026-06-01T12:34:56Z\",\n            \"location\": {\"lat\": -1.234, \"lon\": 36.789},\n            \"additional\": {\"battery_voltage\": 3.9},\n        },\n    ])\n    print(response)\n\n\nasyncio.run(main())\n</code></pre> <p>Session lifetime</p> <p>Unlike <code>GundiClient</code>, <code>GundiDataSenderClient</code> is not an async context manager \u2014 it doesn't hold a persistent session. Instantiate it directly and <code>await</code> its methods.</p>"},{"location":"sending-data/observations/#sending-multiple-observations","title":"Sending multiple observations","text":"<p>Pass a list with as many dicts as you like. All items are submitted in a single request:</p> <pre><code>response = await sender.post_observations(data=[\n    {\"source\": \"device-001\", \"recorded_at\": \"...\", ...},\n    {\"source\": \"device-002\", \"recorded_at\": \"...\", ...},\n])\n</code></pre> <p>Rate limits</p> <p>Very large batches may hit per-deployment rate limits. Check your Gundi deployment's documentation for the recommended batch ceiling.</p>"},{"location":"sending-data/observations/#required-and-common-fields","title":"Required and common fields","text":"Field Required Notes <code>source</code> Yes Device identifier \u2014 typically the source's ID at the provider <code>type</code> Yes <code>tracking-device</code>, <code>stationary-object</code>, <code>ranger-radio-position</code>, etc. <code>recorded_at</code> Yes ISO-8601 timestamp <code>location</code> Yes <code>{\"lat\": float, \"lon\": float}</code> <code>subject_type</code> Recommended Helps the destination categorize the source <code>additional</code> Optional Free-form dict of additional attributes <p>Schema is destination-defined</p> <p>Gundi forwards what it receives (potentially through a Route's transformation). Refer to your destination's documentation for the full accepted schema.</p>"},{"location":"sending-data/observations/#the-response","title":"The response","text":"<p><code>post_observations</code> returns the raw JSON the API returned \u2014 typically a list where each item contains the Gundi-assigned <code>object_id</code> and <code>created_at</code> timestamp:</p> <pre><code>[\n    {\n        \"object_id\": \"96422ba3-3ea9-4b0d-8950-850e218e1140\",\n        \"created_at\": \"2026-06-01T12:34:09.539777Z\"\n    }\n]\n</code></pre> <p>The exact shape may vary by deployment.</p>"},{"location":"sending-data/observations/#error-handling","title":"Error handling","text":"<p>Wrap calls in a <code>try/except</code> block to catch API errors:</p> <pre><code>from gundi_client_v2.errors import GundiAPIError\n\ntry:\n    response = await sender.post_observations(data=[...])\nexcept GundiAPIError as exc:\n    print(f\"Gundi API error {exc.status_code}: {exc}\")\n</code></pre> <p><code>GundiDataSenderClient</code> uses a fixed 120-second HTTPX timeout. If your deployment is slow to respond, consider splitting large batches into smaller ones.</p> <p>See Error handling for the full pattern and a list of exception types.</p>"},{"location":"sending-data/observations/#related","title":"Related","text":"<ul> <li>Payload types \u2014 overview of observations, events, and messages</li> <li>Sending Events</li> <li>Sending Messages and Attachments</li> </ul>"}]}